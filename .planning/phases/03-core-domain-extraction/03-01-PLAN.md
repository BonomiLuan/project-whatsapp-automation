---
phase: 03-core-domain-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/usecases/FilterDeals.ts
  - src/core/usecases/CategoryRotation.ts
  - src/core/usecases/helpers/formatMessage.ts
  - tests/usecases/FilterDeals.test.ts
  - tests/usecases/CategoryRotation.test.ts
  - tests/usecases/formatMessage.test.ts
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-03, EXTRACT-04]
must_haves:
  truths:
    - "Pure business logic (filtering, category rotation, message formatting) lives in src/core/usecases/ with zero IO imports"
    - "Characterization tests exist and pass for all three extracted pure functions BEFORE any god-file is split"
    - "filterDeals removes Prime-exclusive / app-only / below-min-temperature / disallowed-store deals exactly as the inline pelando logic does today"
    - "nextCategory round-robins through ALL_CATEGORIES and preferDifferentSource avoids repeating the last source"
    - "formatMessage produces the same Telegram caption string as the inline TelegramPublisher.formatMessage"
  artifacts:
    - path: "src/core/usecases/FilterDeals.ts"
      provides: "Pure filterDeals(deals, filter) function"
      contains: "export function filterDeals"
    - path: "src/core/usecases/CategoryRotation.ts"
      provides: "Pure nextCategory + preferDifferentSource functions"
      contains: "export function nextCategory"
    - path: "src/core/usecases/helpers/formatMessage.ts"
      provides: "Pure Telegram caption builder"
      contains: "export function formatMessage"
    - path: "tests/usecases/FilterDeals.test.ts"
      provides: "Characterization tests for filterDeals"
    - path: "tests/usecases/CategoryRotation.test.ts"
      provides: "Characterization tests for rotation logic"
    - path: "tests/usecases/formatMessage.test.ts"
      provides: "Golden-master tests for caption formatting"
  key_links:
    - from: "src/core/usecases/FilterDeals.ts"
      to: "src/core/domain/Deal.ts"
      via: "import type Deal, Filter"
      pattern: "import type.*Deal"
---

<objective>
Extract the three pure-logic units buried inside the god-files into testable `src/core/usecases/` functions, each protected by characterization tests written and passing in the same plan. This plan is the safety net that decision D-01c mandates: no god-file may be split until these pure functions are tested. It produces NEW files only — it does NOT modify any god-file yet (that happens in Wave 3, once the use cases and adapters consume these helpers).

Purpose: Satisfy D-01 (characterization tests before split), EXTRACT-04 (FilterDeals), and lay the pure-function foundation for SuggestDeals (D-03) and TelegramPublisher's DealPublisher adoption (D-05).
Output: 3 pure source files under `src/core/usecases/` + 3 Vitest test files under `tests/usecases/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-domain-extraction/03-CONTEXT.md
@.planning/phases/03-core-domain-extraction/03-PATTERNS.md

# Canonical spec — read the FilterDeals / CategoryRotation / formatMessage sections before coding
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md

# Source of the inline logic being characterized (READ — do not modify in this plan)
@src/adapters/scrapers/PelandoScraper.ts
@src/web/server.ts
@src/adapters/publishers/TelegramPublisher.ts
@src/adapters/publishers/format.ts

<interfaces>
From src/core/domain/Deal.ts:
  export type Marketplace = 'pelando' | 'mercadolivre' | 'shopee' | 'amazon'
  export type Deal = { id; title; price: number; originalPrice?: number; url; imageUrl?; couponCode?; marketplace: Marketplace; category: string; postedAt: Date }

From src/core/domain/Filter.ts:
  export type Filter = { keywords: string[]; excludeKeywords: string[]; minDiscount: number; categories: string[] }

From src/core/ports/RotationStore.ts:
  export type RotationCursor = { roundRobinIndex: number; lastSource: string | null }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract filterDeals as a pure, characterized function</name>
  <read_first>
    - src/core/domain/Deal.ts (Deal/Marketplace shape)
    - src/core/domain/Filter.ts (Filter shape)
    - src/adapters/scrapers/PelandoScraper.ts lines 521-651 (inline filter rules: MIN_TEMPERATURE=20, ALLOWED_STORES, Prime/app-only regex `/só no app|somente no app|only in app|\bprime\b/i`)
    - src/adapters/publishers/format.ts (pure-function style to copy)
    - tests/links.test.ts (Vitest structure, `.js` import convention)
  </read_first>
  <behavior>
    - Empty filter (all arrays empty, minDiscount 0) returns all deals unchanged.
    - When filter.categories is non-empty, deals whose `category` is not in the list are removed.
    - When filter.excludeKeywords contains a token, any deal whose lowercased title includes that token (case-insensitive) is removed (covers the `prime` / `só no app` exclusion intent).
    - When filter.keywords is non-empty, only deals whose lowercased title includes at least one keyword survive.
    - When filter.minDiscount > 0 and a deal has originalPrice, deals whose computed discount `((originalPrice - price)/originalPrice)*100` is below minDiscount are removed; deals without originalPrice pass the discount gate.
    - Determinism: same input array + filter always yields the same output order (input order preserved).
  </behavior>
  <action>
    Create `src/core/usecases/FilterDeals.ts` exporting a pure named function `filterDeals(deals: Deal[], filter: Filter): Deal[]` following the FilterDeals section of the spec and the format.ts pure-function style (no IO imports — no pg/axios/fs, no module-level mutable state, all inputs as parameters). Import `Deal` from `../domain/Deal.js` and `Filter` from `../domain/Filter.js` (note `.js` extension on local imports). Implement category match, keyword inclusion, excludeKeyword exclusion (lowercased substring), and minDiscount percentage gate exactly as described in <behavior>. Then create `tests/usecases/FilterDeals.test.ts` using `import { describe, it, expect } from 'vitest'` and `import { filterDeals } from '../../src/core/usecases/FilterDeals.js'`, with one `it()` per <behavior> bullet using small inline `Deal` fixtures. Do NOT modify PelandoScraper.ts in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/usecases/FilterDeals.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/usecases/FilterDeals.ts` contains `export function filterDeals(`
    - `grep -v '^//' src/core/usecases/FilterDeals.ts | grep -E "from 'pg'|from 'axios'|from 'fs'|require\(" | wc -l` returns 0 (no IO imports)
    - `npx vitest run tests/usecases/FilterDeals.test.ts` exits 0 with at least 6 passing tests
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>filterDeals is a pure function with passing characterization tests covering every <behavior> bullet; no god-file modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract CategoryRotation (nextCategory + preferDifferentSource) as pure functions</name>
  <read_first>
    - src/web/server.ts lines 577-646 (roundRobinIndex / ALL_CATEGORIES walk / lastSentSource variety logic — the source of truth)
    - src/core/ports/RotationStore.ts (RotationCursor type)
    - src/core/usecases/FilterDeals.ts (pure style established in Task 1)
  </read_first>
  <behavior>
    - nextCategory({roundRobinIndex:0, lastSource:null}, ['a','b','c']) returns category 'a' and nextCursor.roundRobinIndex === 1.
    - nextCategory wraps: roundRobinIndex 3 with 3 categories returns category at index 0.
    - nextCursor preserves lastSource unchanged (only roundRobinIndex advances).
    - preferDifferentSource(candidates, null) returns candidates unchanged.
    - preferDifferentSource filters out candidates whose marketplace === lastSource when at least one different-source candidate exists.
    - preferDifferentSource returns the original candidates when ALL candidates share the lastSource (no viable alternative).
  </behavior>
  <action>
    Create `src/core/usecases/CategoryRotation.ts` exporting two pure named functions matching the CategoryRotation section of 03-PATTERNS.md: `nextCategory(cursor: RotationCursor, allCategories: string[]): { nextCursor: RotationCursor; category: string }` and `preferDifferentSource<T extends { marketplace: string }>(candidates: T[], lastSource: string | null): T[]`. Import `RotationCursor` from `../ports/RotationStore.js`. No IO, no module-level state, no randomness inside these functions (random pick stays in the use case caller). Then create `tests/usecases/CategoryRotation.test.ts` with one `it()` per <behavior> bullet. Do NOT modify server.ts in this plan.
  </action>
  <verify>
    <automated>npx vitest run tests/usecases/CategoryRotation.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/usecases/CategoryRotation.ts` contains `export function nextCategory(` and `export function preferDifferentSource`
    - `grep -v '^//' src/core/usecases/CategoryRotation.ts | grep -E "from 'pg'|from 'axios'|Math.random" | wc -l` returns 0
    - `npx vitest run tests/usecases/CategoryRotation.test.ts` exits 0 with at least 6 passing tests
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>nextCategory and preferDifferentSource are pure, deterministic, and fully tested; server.ts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract formatMessage Telegram caption builder as a pure helper</name>
  <read_first>
    - src/adapters/publishers/TelegramPublisher.ts lines 32-63 (inline formatMessage — the exact string template to replicate)
    - src/adapters/publishers/format.ts (pure helper style + exported fmt)
    - src/core/usecases/FilterDeals.ts (pure style established in Task 1)
  </read_first>
  <behavior>
    - formatMessage with originalPrice AND price renders the line `~{originalPrice}~ por *{price}*`.
    - formatMessage with only price renders `*{price}*`.
    - formatMessage with a coupon renders a `🎟️ Cupom: *{coupon}*` line; without coupon that line is absent.
    - formatMessage with groupUrl includes the `💬 Link para o grupo:` line + the url; without groupUrl those lines are absent.
    - Output always starts with `{emoji} *{title}*` and ends with `#Anúncio` after trimming.
  </behavior>
  <action>
    Create `src/core/usecases/helpers/formatMessage.ts` exporting a pure named function `formatMessage(args: { emoji: string; title: string; originalPrice?: string; price?: string; coupon?: string; buyUrl: string; groupUrl: string }): string` whose output is byte-identical to the inline `formatMessage` in TelegramPublisher.ts lines 32-63 (same array of lines, same `.filter(...).join('\n').trim()`). No IO imports. Then create `tests/usecases/formatMessage.test.ts` as golden-master tests: assert exact substrings/lines per <behavior> bullet. Do NOT modify TelegramPublisher.ts in this plan — that consumer swap happens in Wave 2/3.
  </action>
  <verify>
    <automated>npx vitest run tests/usecases/formatMessage.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/usecases/helpers/formatMessage.ts` contains `export function formatMessage(`
    - `grep -v '^//' src/core/usecases/helpers/formatMessage.ts | grep -E "from 'axios'|from 'telegraf'|from 'pg'" | wc -l` returns 0
    - `npx vitest run tests/usecases/formatMessage.test.ts` exits 0 with at least 5 passing tests
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>formatMessage is a pure helper producing the same caption string as today's inline version, fully tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped deal data → filterDeals | Untrusted title/price strings from external sites flow into pure logic |
| (none new) | This plan adds no new network, DB, or process-install surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | filterDeals title/keyword matching | mitigate | All matching is read-only substring compare on lowercased strings; no eval, no regex built from untrusted input |
| T-03-02 | Denial of Service | formatMessage with very long title | accept | Title length is bounded upstream by scrapers (`.slice(0,80)`); pure string concat, no catastrophic backtracking |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; if any install becomes necessary it requires the Package Legitimacy Gate + blocking human checkpoint |
</threat_model>

<verification>
- `npx vitest run tests/usecases/` — all three suites pass.
- `npx tsc --noEmit` — exits 0.
- No file under `src/core/` imports from `src/adapters/`, `src/web/`, or `src/jobs/`: `grep -rE "from '\\.\\./\\.\\./(adapters|web|jobs)" src/core/ | wc -l` returns 0.
- No god-file was modified by this plan (git diff touches only the 6 declared files).
</verification>

<success_criteria>
filterDeals, nextCategory/preferDifferentSource, and formatMessage exist as pure functions in `src/core/usecases/` with passing Vitest characterization suites, and no god-file was touched. This is the D-01c safety net that unblocks Wave 2/3 splits.
</success_criteria>

<output>
Create `.planning/phases/03-core-domain-extraction/03-01-SUMMARY.md` when done.
</output>
