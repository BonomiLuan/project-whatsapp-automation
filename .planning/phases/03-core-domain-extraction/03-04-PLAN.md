---
phase: 03-core-domain-extraction
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/adapters/publishers/TelegramPublisher.ts
  - src/adapters/scrapers/PelandoScraper.ts
  - src/adapters/scrapers/MercadoLivreScraper.ts
  - tests/adapters/toDeal.test.ts
autonomous: true
requirements: [EXTRACT-01, EXTRACT-03, EXTRACT-07]
must_haves:
  truths:
    - "TelegramPublisher implements DealPublisher and exposes publish(deal: Deal, tenant: Tenant) using a private toDeal mapping; UnifiedDeal stays internal to the adapter"
    - "PelandoScraper and MercadoLivreScraper implement DealScraper and return Deal[] via a private toDeal() conversion; their internal raw types do not leak to the core"
    - "TelegramPublisher consumes the pure formatMessage helper instead of its inline copy (no business logic duplicated)"
    - "The toDeal mappings are covered by characterization tests over fixture raw objects"
  artifacts:
    - path: "src/adapters/publishers/TelegramPublisher.ts"
      provides: "TelegramPublisher implements DealPublisher + toDeal"
      contains: "implements DealPublisher"
    - path: "src/adapters/scrapers/PelandoScraper.ts"
      provides: "PelandoScraper implements DealScraper"
      contains: "implements DealScraper"
    - path: "src/adapters/scrapers/MercadoLivreScraper.ts"
      provides: "MercadoLivreScraper implements DealScraper"
      contains: "implements DealScraper"
    - path: "tests/adapters/toDeal.test.ts"
      provides: "Characterization tests for raw→Deal mappings"
  key_links:
    - from: "src/adapters/publishers/TelegramPublisher.ts"
      to: "src/core/ports/DealPublisher.ts"
      via: "implements DealPublisher"
      pattern: "implements DealPublisher"
    - from: "src/adapters/publishers/TelegramPublisher.ts"
      to: "src/core/usecases/helpers/formatMessage.ts"
      via: "import formatMessage"
      pattern: "formatMessage"
---

<objective>
Make the three god-file adapters conform to their ports without changing observable behavior: `TelegramPublisher implements DealPublisher` (EXTRACT-07, D-05a) with a private `toDeal()` so `UnifiedDeal` never leaks into the core (D-02), and `PelandoScraper` / `MercadoLivreScraper implements DealScraper` (D-05b) returning `Deal[]` via their own `toDeal()`. TelegramPublisher also switches from its inline `formatMessage` to the pure helper extracted in 03-01 (EXTRACT-01/03 — removing duplicated formatting logic, characterized in Wave 1). This plan depends on 03-01 (the formatMessage helper + its golden-master test) but NOT on 03-02/03-03, so it runs in Wave 2 in parallel with 03-03.

Purpose: EXTRACT-07 (DealPublisher impl), D-05a/D-05b (implements adoption), D-02 (toDeal keeps UnifiedDeal internal), EXTRACT-01/03 (replace characterized inline formatter with the pure helper).
Output: 3 modified adapters + 1 toDeal characterization test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-domain-extraction/03-CONTEXT.md
@.planning/phases/03-core-domain-extraction/03-PATTERNS.md
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md

@src/adapters/publishers/TelegramPublisher.ts
@src/adapters/scrapers/PelandoScraper.ts
@src/adapters/scrapers/MercadoLivreScraper.ts

# Wave 1 dependency
@src/core/usecases/helpers/formatMessage.ts

<interfaces>
From src/core/ports/DealPublisher.ts: publish(deal: Deal, tenant: Tenant): Promise<void>
From src/core/ports/DealScraper.ts:   fetchDeals(category?: string): Promise<Deal[]>
From src/core/domain/Deal.ts:  Marketplace = 'pelando'|'mercadolivre'|'shopee'|'amazon'; Deal = { id; title; price: number; originalPrice?: number; url; imageUrl?; couponCode?; marketplace: Marketplace; category: string; postedAt: Date }
From src/core/usecases/helpers/formatMessage.ts: formatMessage(args): string
Internal adapter types (stay private, do NOT export to core):
  src/web/server.ts: UnifiedDeal (source: 'shopee'|'amazon'|'mercado-livre', price: string, ...)
  src/adapters/scrapers/PelandoScraper.ts: PelandoDeal (price: string, temperature, couponCode, dealUrl, ...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TelegramPublisher implements DealPublisher + toDeal + use pure formatMessage</name>
  <read_first>
    - src/adapters/publishers/TelegramPublisher.ts (full — esp. lines 32-63 inline formatMessage, 872-941 sendDealToChat, the UnifiedDeal import from server.ts line 12)
    - src/core/ports/DealPublisher.ts (publish signature)
    - src/core/domain/Deal.ts + src/core/domain/Tenant.ts (target types)
    - src/core/usecases/helpers/formatMessage.ts (pure helper to import)
    - src/web/server.ts lines 175-190 (UnifiedDeal definition — the raw type to map FROM, keep internal)
    - 03-PATTERNS.md TelegramPublisher section (implements + toDeal pattern, D-02a mapping example)
  </read_first>
  <action>
    In `src/adapters/publishers/TelegramPublisher.ts`: (1) ADD `import type { DealPublisher } from '../../core/ports/DealPublisher.js'`, `import type { Deal, Marketplace } from '../../core/domain/Deal.js'`, `import type { Tenant } from '../../core/domain/Tenant.js'`, and `import { formatMessage } from '../../core/usecases/helpers/formatMessage.js'`. (2) Replace the local inline `function formatMessage({...})` (lines 32-63) with the imported pure helper — delete the duplicate definition so there is exactly one formatMessage in the module (the imported one); all existing call sites (buildTelegramText, sendDealToChat) keep compiling unchanged. (3) Wrap the existing publishing logic in `export class TelegramPublisher implements DealPublisher` exposing `async publish(deal: Deal, tenant: Tenant): Promise<void>` — internally it converts the core `Deal` back to the UnifiedDeal shape needed by the existing `sendDealToChat` path (or calls the shared send logic directly). (4) Add a private `toDeal(raw: UnifiedDeal): Deal` mapping per 03-PATTERNS.md (id, title, price: parse number, originalPrice, url: affiliateUrl, imageUrl, marketplace: map 'mercado-livre'→'mercadolivre', category, postedAt: new Date(publishedAt)) — keep `UnifiedDeal` referenced only inside this module, never re-exported as a core type. Preserve every existing exported function (createBot, sendDealToChat, sendProductToChat, sendPelandoCouponToChat) so server.ts/jobs keep working until Wave 3 rewires them.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/usecases/formatMessage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/publishers/TelegramPublisher.ts` contains `class TelegramPublisher implements DealPublisher`
    - File imports `formatMessage` from `../../core/usecases/helpers/formatMessage.js`
    - `grep -c "^function formatMessage" src/adapters/publishers/TelegramPublisher.ts` returns 0 (inline duplicate removed)
    - File contains a private `toDeal(` method and maps `'mercado-livre'` to `'mercadolivre'`
    - Existing exports `createBot`, `sendDealToChat`, `sendProductToChat`, `sendPelandoCouponToChat` are still present
    - `npx tsc --noEmit` exits 0 and the formatMessage golden-master suite still passes
  </acceptance_criteria>
  <done>TelegramPublisher implements DealPublisher, uses the single pure formatMessage helper, and keeps UnifiedDeal internal via toDeal — with no behavior change and all legacy exports intact.</done>
</task>

<task type="auto">
  <name>Task 2: PelandoScraper + MercadoLivreScraper implement DealScraper with toDeal</name>
  <read_first>
    - src/adapters/scrapers/PelandoScraper.ts (full — esp. PelandoDeal interface lines 495-505 and fetchDeals lines 525-652)
    - src/adapters/scrapers/MercadoLivreScraper.ts (full — its raw deal shape + fetchMLCategoryDeals)
    - src/core/ports/DealScraper.ts + src/core/domain/Deal.ts
    - 03-PATTERNS.md PelandoScraper / MercadoLivreScraper sections
  </read_first>
  <action>
    In `src/adapters/scrapers/PelandoScraper.ts`: ADD `import type { DealScraper } from '../../core/ports/DealScraper.js'` and `import type { Deal, Marketplace } from '../../core/domain/Deal.js'`. Introduce `export class PelandoScraper implements DealScraper` whose `async fetchDeals(category?: string): Promise<Deal[]>` calls the existing scraping pipeline and maps each `PelandoDeal` to a core `Deal` via a private `toDeal(raw: PelandoDeal): Deal` (id, title, price: numeric parse of the `price` string, url: dealUrl, imageUrl, couponCode: raw.couponCode || undefined, marketplace inferred from store ('amazon'/'mercadolivre'/'shopee'/'pelando'), category: raw category or 'geral', postedAt: new Date(publishedAt)). Keep the existing standalone `export async function fetchDeals` so server.ts keeps importing it until Wave 3. Do the same for `src/adapters/scrapers/MercadoLivreScraper.ts`: `export class MercadoLivreScraper implements DealScraper` with a private `toDeal` mapping its raw ML deal to a `Deal` with `marketplace: 'mercadolivre'`; retain existing exports (e.g. `fetchMLCategoryDeals`). Then create `tests/adapters/toDeal.test.ts` characterizing both mappings over hand-built raw fixtures: assert numeric price parsing, the `'mercado-livre'`/store→Marketplace mapping, couponCode passthrough, and that an empty couponCode string becomes undefined. (To test the private toDeal without network, either expose a `static toDeal`/exported pure `toDeal` function the class delegates to, or instantiate and call a thin public mapper — choose the exported-pure-function approach to keep the test IO-free.)
  </action>
  <verify>
    <automated>npx vitest run tests/adapters/toDeal.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/scrapers/PelandoScraper.ts` contains `class PelandoScraper implements DealScraper`
    - `src/adapters/scrapers/MercadoLivreScraper.ts` contains `class MercadoLivreScraper implements DealScraper`
    - Both files still export their existing standalone functions (`fetchDeals`, `fetchMLCategoryDeals`)
    - `tests/adapters/toDeal.test.ts` passes with at least 4 tests covering price parsing, marketplace mapping, and couponCode handling
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both scrapers implement DealScraper and return Deal[] via tested toDeal mappings; raw scraper types stay internal; legacy function exports preserved for Wave 3 rewiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external site HTML/API → scraper toDeal | Untrusted scraped fields parsed into typed Deal |
| adapter → core | Adapter must not leak UnifiedDeal/PelandoDeal raw types into the core domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | toDeal price parsing of untrusted strings | mitigate | Use defensive numeric parse (NaN→0 or skip) in toDeal; characterization test covers malformed price input |
| T-03-10 | Information Disclosure | leaking internal UnifiedDeal/PelandoDeal into core types | mitigate | Raw types referenced only inside adapter modules; core imports only domain Deal; tsc + grep gate enforce direction |
| T-03-11 | Integrity | formatMessage swap changing the published caption | mitigate | TelegramPublisher reuses the byte-identical pure helper validated by 03-01 golden-master suite (re-run in verify) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; any future install requires Package Legitimacy Gate + blocking human checkpoint |
</threat_model>

<verification>
- `npx vitest run tests/adapters/toDeal.test.ts tests/usecases/formatMessage.test.ts` — pass.
- `npx tsc --noEmit` exits 0.
- Core purity preserved: `grep -rE "UnifiedDeal|PelandoDeal" src/core/ | wc -l` returns 0.
- All previously-exported functions in the three adapters remain exported (server.ts/jobs still compile).
</verification>

<success_criteria>
TelegramPublisher implements DealPublisher (using the pure formatMessage helper), and both scrapers implement DealScraper returning Deal[] via tested toDeal mappings — with UnifiedDeal/PelandoDeal kept internal and zero behavior change. Legacy exports stay intact so Wave 3 can rewire safely.
</success_criteria>

<output>
Create `.planning/phases/03-core-domain-extraction/03-04-SUMMARY.md` when done.
</output>
