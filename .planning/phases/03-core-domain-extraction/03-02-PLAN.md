---
phase: 03-core-domain-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/lock/PgAdvisoryLock.ts
  - src/adapters/store/InMemoryRotationStore.ts
  - src/adapters/db/PgDealRepository.ts
  - src/adapters/db/PgTenantRepository.ts
  - tests/adapters/PgAdvisoryLock.test.ts
  - tests/adapters/InMemoryRotationStore.test.ts
autonomous: true
requirements: [EXTRACT-06]
must_haves:
  truths:
    - "A PgAdvisoryLock class implements the Lock port and converts string keys to stable integer lock IDs via a deterministic hash"
    - "An InMemoryRotationStore class implements the RotationStore port, preserving today's roundRobinIndex/lastSource behavior in memory"
    - "PgDealRepository.findRecentlySentIds and markAsSent run real SQL against deal_history keyed by (deal_id, tenant_id) instead of throwing"
    - "PgTenantRepository methods run real SQL instead of throwing the Phase-3 stub error"
  artifacts:
    - path: "src/adapters/lock/PgAdvisoryLock.ts"
      provides: "PgAdvisoryLock class implements Lock + existing withCronLock retained"
      contains: "class PgAdvisoryLock implements Lock"
    - path: "src/adapters/store/InMemoryRotationStore.ts"
      provides: "InMemoryRotationStore implements RotationStore"
      contains: "class InMemoryRotationStore implements RotationStore"
    - path: "src/adapters/db/PgDealRepository.ts"
      provides: "Real DealRepository over deal_history"
      contains: "pool.query"
    - path: "src/adapters/db/PgTenantRepository.ts"
      provides: "Real TenantRepository SQL"
      contains: "pool.query"
  key_links:
    - from: "src/adapters/lock/PgAdvisoryLock.ts"
      to: "src/core/ports/Lock.ts"
      via: "implements Lock"
      pattern: "implements Lock"
    - from: "src/adapters/db/PgDealRepository.ts"
      to: "deal_history table"
      via: "pool.query SELECT/INSERT"
      pattern: "deal_history"
---

<objective>
Turn the leaf adapter stubs into real `implements`-backed adapters that the Wave 2 use cases and Wave 3 composition root will consume: a `PgAdvisoryLock` class implementing the `Lock` port (D-04), an `InMemoryRotationStore` implementing `RotationStore` (D-03b), and real SQL bodies for `PgDealRepository` (EXTRACT-06) and `PgTenantRepository` (D-05c). These adapters have no dependency on the god-files, so this plan runs in parallel with 03-01.

Purpose: D-04 (Lock port adapter), D-03b (in-memory rotation store), EXTRACT-06 + D-05c (real Pg repositories).
Output: 4 adapter files (2 new behavior, 2 stub-to-real) + 2 Vitest suites for the IO-free adapters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-domain-extraction/03-CONTEXT.md
@.planning/phases/03-core-domain-extraction/03-PATTERNS.md
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md

@src/adapters/lock/PgAdvisoryLock.ts
@src/adapters/db/PgDealRepository.ts
@src/adapters/db/PgTenantRepository.ts
@src/adapters/db/pool.ts

<interfaces>
From src/core/ports/Lock.ts:
  export interface Lock { withLock<T>(key: string, fn: () => Promise<T>): Promise<T | null> }
From src/core/ports/RotationStore.ts:
  export type RotationCursor = { roundRobinIndex: number; lastSource: string | null }
  export interface RotationStore { load(tenantId): Promise<RotationCursor>; save(tenantId, cursor): Promise<void>; reset(tenantId): Promise<void> }
From src/core/ports/DealRepository.ts:
  export interface DealRepository { findRecentlySentIds(tenantId, withinDays): Promise<Set<string>>; markAsSent(dealId, tenantId): Promise<void> }
From src/core/ports/TenantRepository.ts:
  export interface TenantRepository { findAll(): Promise<Tenant[]>; findById(id): Promise<Tenant | null>; save(tenant): Promise<void> }
From src/adapters/db/pool.ts:
  export const pool  // pg Pool; supports pool.query(text, params)
From src/adapters/db/PgLinkRepository.ts (existing, reference dedupe behavior):
  recordDealSent(id, title, category); getExcludedDealIds(withinDays)  // current 7-day dedupe over deal_history
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PgAdvisoryLock class implements Lock + InMemoryRotationStore</name>
  <read_first>
    - src/adapters/lock/PgAdvisoryLock.ts (existing withCronLock/claimAutoSendSlot — keep them, do not delete)
    - src/core/ports/Lock.ts (port signature)
    - src/core/ports/RotationStore.ts (RotationCursor + port)
    - src/adapters/scheduler/NodeCronScheduler.ts (the only existing complete `implements` example)
    - src/adapters/db/HistoryRepository.ts (in-memory state pattern)
  </read_first>
  <behavior>
    - PgAdvisoryLock.hashKey is deterministic: same string key always yields the same integer in range [0, 2^31).
    - PgAdvisoryLock.withLock('monitor:pelando', fn) delegates to withCronLock(hashedId, fn) and returns fn()'s result, or null when the advisory lock is not acquired.
    - InMemoryRotationStore.load returns {roundRobinIndex:0, lastSource:null} for an unknown tenantId.
    - InMemoryRotationStore.save then load round-trips the saved cursor for the same tenantId.
    - InMemoryRotationStore.reset sets the cursor back to {roundRobinIndex:0, lastSource:null}.
  </behavior>
  <action>
    In `src/adapters/lock/PgAdvisoryLock.ts`, ADD (do not remove `withCronLock`/`claimAutoSendSlot`) `import type { Lock } from '../../core/ports/Lock.js'` and a class `export class PgAdvisoryLock implements Lock` with a private `hashKey(key: string): number` using the FNV-32 algorithm shown in 03-PATTERNS.md (seed 0x811c9dc5, prime 0x01000193, `>>> 0`, `% (2 ** 31)`) and an `async withLock<T>(key, fn)` that calls `withCronLock(this.hashKey(key), fn)`. Create new file `src/adapters/store/InMemoryRotationStore.ts` with `export class InMemoryRotationStore implements RotationStore` backed by a `private state = new Map<string, RotationCursor>()` exactly as in 03-PATTERNS.md (import RotationStore + RotationCursor from `../../core/ports/RotationStore.js`). Then create `tests/adapters/PgAdvisoryLock.test.ts` (test hashKey determinism + range only — do NOT hit Postgres) and `tests/adapters/InMemoryRotationStore.test.ts` (load default / save+load round-trip / reset). To make hashKey testable without DB, expose it as a public or `static` method, or test withLock with a fake by extracting hashKey to a standalone exported `export function hashKey(key: string): number` in the same module and have the class call it.
  </action>
  <verify>
    <automated>npx vitest run tests/adapters/PgAdvisoryLock.test.ts tests/adapters/InMemoryRotationStore.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/lock/PgAdvisoryLock.ts` contains `class PgAdvisoryLock implements Lock` AND still contains `export async function withCronLock`
    - `src/adapters/store/InMemoryRotationStore.ts` contains `class InMemoryRotationStore implements RotationStore`
    - `npx vitest run tests/adapters/PgAdvisoryLock.test.ts tests/adapters/InMemoryRotationStore.test.ts` exits 0 with at least 5 passing tests
    - hashKey returns identical values across two calls with the same input (asserted in test)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PgAdvisoryLock implements Lock with a deterministic hash; InMemoryRotationStore implements RotationStore; both have passing IO-free tests; legacy lock helpers retained.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real SQL in PgDealRepository and PgTenantRepository</name>
  <read_first>
    - src/adapters/db/PgDealRepository.ts (current stub throwing 'Not implemented — Phase 3')
    - src/adapters/db/PgTenantRepository.ts (current stub)
    - src/adapters/db/pool.ts (pool.query API)
    - src/adapters/db/PgLinkRepository.ts (existing recordDealSent / getExcludedDealIds over deal_history — match column/dedupe semantics; read the relevant functions)
    - src/core/domain/Tenant.ts (Tenant shape to map rows into)
    - docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md (deal_history per-tenant dedupe note, lines ~364; tenants schema)
  </read_first>
  <action>
    In `src/adapters/db/PgDealRepository.ts`: replace both `throw new Error('Not implemented — Phase 3')` bodies with real `pool.query` calls (import `pool` from `./pool.js`). `findRecentlySentIds(tenantId, withinDays)` runs `SELECT deal_id FROM deal_history WHERE tenant_id = $1 AND sent_at > NOW() - ($2 || ' days')::INTERVAL` and returns `new Set(rows.map(r => r.deal_id))`. `markAsSent(dealId, tenantId)` runs `INSERT INTO deal_history (deal_id, tenant_id, sent_at) VALUES ($1, $2, NOW()) ON CONFLICT DO NOTHING`. Because the multi-tenant deal_history schema (tenant_id column) is not created until Phase 4, guard each query so a missing column/table does not crash production: wrap the body in try/catch returning an empty Set / no-op on error and `console.warn('[PgDealRepository] ...', err)` — mirroring the fallback style already used by getExcludedDealIds. In `src/adapters/db/PgTenantRepository.ts`: replace the three stub throws with real `pool.query` against the `tenants` family of tables, mapping rows to `Tenant` (id, name, active; channels/filters/affiliates default to empty arrays/objects when their tables are absent). Same defensive try/catch: `findAll` returns `[]` on error, `findById` returns `null`, `save` is a no-op log on error — so production (Phase 3, no tenants table yet) never throws at boot.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Not implemented — Phase 3" src/adapters/db/PgDealRepository.ts` returns 0
    - `grep -c "Not implemented — Phase 3" src/adapters/db/PgTenantRepository.ts` returns 0
    - `src/adapters/db/PgDealRepository.ts` contains `deal_history` and `pool.query`
    - `src/adapters/db/PgTenantRepository.ts` contains `pool.query` and maps rows to `Tenant`
    - Both files wrap queries in try/catch with a console.warn fallback (no unguarded throw path)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PgDealRepository and PgTenantRepository execute real SQL with defensive fallbacks; neither throws the Phase-3 stub error; production boot is safe even before Phase-4 schema exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| use case → PgAdvisoryLock | String lock keys hashed to integers before reaching Postgres advisory lock |
| use case → Pg*Repository | tenantId / dealId values cross into parameterized SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Injection (Tampering) | PgDealRepository / PgTenantRepository SQL | mitigate | All queries use parameterized `$1/$2` placeholders via pool.query; no string interpolation of dealId/tenantId into SQL |
| T-03-04 | Denial of Service | hashKey collisions causing lock contention | accept | FNV-32 over short fixed keys ('monitor:pelando' etc.) has negligible collision risk for the handful of cron keys; matches existing integer-lock behavior |
| T-03-05 | Denial of Service | missing Phase-4 tables throw at boot | mitigate | Defensive try/catch returns empty Set / [] / null so production stays up before the multi-tenant migration ships |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; any future install requires Package Legitimacy Gate + blocking human checkpoint |
</threat_model>

<verification>
- `npx vitest run tests/adapters/` — lock + rotation store suites pass without a database.
- `npx tsc --noEmit` exits 0.
- `grep -rE "from '\\.\\./\\.\\./\\.\\./(web|jobs)'" src/adapters/ | wc -l` — adapters do not import web/jobs.
- No `throw new Error('Not implemented` remains in the two db adapters.
</verification>

<success_criteria>
PgAdvisoryLock implements Lock (deterministic hash), InMemoryRotationStore implements RotationStore, and both Pg repositories run real, parameterized, defensively-guarded SQL — all consumable by the Wave 2 use cases and Wave 3 wiring without touching any god-file.
</success_criteria>

<output>
Create `.planning/phases/03-core-domain-extraction/03-02-SUMMARY.md` when done.
</output>
