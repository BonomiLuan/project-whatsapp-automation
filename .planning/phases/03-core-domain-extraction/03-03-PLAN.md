---
phase: 03-core-domain-extraction
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/core/usecases/MonitorDeals.ts
  - src/core/usecases/SuggestDeals.ts
  - tests/usecases/MonitorDeals.test.ts
  - tests/usecases/SuggestDeals.test.ts
autonomous: true
requirements: [EXTRACT-05]
must_haves:
  truths:
    - "MonitorDeals lives in src/core/usecases/ and orchestrates scraper→filter→dedupe→publish→markAsSent using only ports, with no IO of its own"
    - "MonitorDeals publishes BEFORE markAsSent (at-least-once) and a single failing deal does not abort the batch"
    - "SuggestDeals lives in src/core/usecases/ and selects the next deal via CategoryRotation + RotationStore, publishing one deal and advancing the cursor only on success"
    - "Both use cases have unit tests using fake ports — no network, no DB, no browser"
  artifacts:
    - path: "src/core/usecases/MonitorDeals.ts"
      provides: "MonitorDeals use case class"
      contains: "class MonitorDeals"
    - path: "src/core/usecases/SuggestDeals.ts"
      provides: "SuggestDeals use case class"
      contains: "class SuggestDeals"
    - path: "tests/usecases/MonitorDeals.test.ts"
      provides: "Unit tests with fake ports"
    - path: "tests/usecases/SuggestDeals.test.ts"
      provides: "Unit tests with fake ports + fake RotationStore"
  key_links:
    - from: "src/core/usecases/MonitorDeals.ts"
      to: "src/core/usecases/FilterDeals.ts"
      via: "import filterDeals"
      pattern: "filterDeals"
    - from: "src/core/usecases/SuggestDeals.ts"
      to: "src/core/usecases/CategoryRotation.ts"
      via: "import nextCategory/preferDifferentSource"
      pattern: "nextCategory|preferDifferentSource"
---

<objective>
Build the two core use cases — `MonitorDeals` (D-03 ordering: scrape→filter→dedupe→publish→mark) and `SuggestDeals` (D-03a, the rotating-showcase logic extracted from `sendNextSuggestion`) — as pure, port-only classes with unit tests driven by fake ports. They consume the Wave 1 pure helpers (filterDeals, CategoryRotation) and the Wave 1 ports (DealRepository, DealPublisher, RotationStore). They do NOT modify any god-file; Wave 3 wires them in and strips the inline logic.

Purpose: EXTRACT-05 (MonitorDeals), D-03a/D-03b (SuggestDeals + CategoryRotation + RotationStore), and the spec's core-testability success criterion (use cases tested without mocks of network/DB).
Output: 2 use case files + 2 fake-port unit test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-domain-extraction/03-CONTEXT.md
@.planning/phases/03-core-domain-extraction/03-PATTERNS.md

# Spec — read MonitorDeals (lines ~199-236) and SuggestDeals (lines ~252-281) before coding
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md

# Source of the SuggestDeals logic being formalized (READ — not modified here)
@src/web/server.ts

# Wave 1 outputs this plan consumes
@src/core/usecases/FilterDeals.ts
@src/core/usecases/CategoryRotation.ts

<interfaces>
From src/core/ports/DealScraper.ts:    fetchDeals(category?): Promise<Deal[]>
From src/core/ports/DealPublisher.ts:  publish(deal: Deal, tenant: Tenant): Promise<void>
From src/core/ports/DealRepository.ts: findRecentlySentIds(tenantId, withinDays): Promise<Set<string>>; markAsSent(dealId, tenantId): Promise<void>
From src/core/ports/TenantRepository.ts: findAll(): Promise<Tenant[]>
From src/core/ports/RotationStore.ts:  load(tenantId): Promise<RotationCursor>; save(tenantId, cursor): Promise<void>; reset(tenantId): Promise<void>
  RotationCursor = { roundRobinIndex: number; lastSource: string | null }
From src/core/domain/Deal.ts:  Deal = { id; title; price; ...; marketplace; category; postedAt }
From src/core/domain/Tenant.ts: Tenant = { id; name; channels; filters: Filter; affiliates; active }
From src/core/usecases/FilterDeals.ts: filterDeals(deals, filter): Deal[]
From src/core/usecases/CategoryRotation.ts: nextCategory(cursor, allCategories); preferDifferentSource(candidates, lastSource)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract MonitorDeals use case with fake-port unit tests</name>
  <read_first>
    - docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md lines 199-236 (MonitorDeals reference incl. at-least-once + per-deal try/catch)
    - src/core/usecases/FilterDeals.ts (filterDeals signature)
    - src/core/ports/DealScraper.ts, DealPublisher.ts, DealRepository.ts, TenantRepository.ts
    - 03-PATTERNS.md MonitorDeals section (constructor-injection pattern)
  </read_first>
  <behavior>
    - execute() fetches deals once via scraper, then iterates only over `tenants.filter(t => t.active)`.
    - For each active tenant: excludes deals whose id is in findRecentlySentIds(tenant.id, 7), applies filterDeals(fresh, tenant.filters), then for each surviving deal calls publish THEN markAsSent (in that order).
    - markAsSent is never called for a deal whose publish threw.
    - A publish that throws for one deal is caught (console.error) and the loop continues to the next deal — the batch is not aborted.
    - Inactive tenants receive zero publish calls.
  </behavior>
  <action>
    Create `src/core/usecases/MonitorDeals.ts` exporting `export class MonitorDeals` with constructor injection of `DealScraper`, `DealPublisher`, `DealRepository`, `TenantRepository` (all `import type` from `../ports/*.js`) and an `async execute(category?: string): Promise<void>` matching the spec's MonitorDeals body and 03-PATTERNS.md: fetch once, loop active tenants, dedupe via `findRecentlySentIds(tenant.id, 7)`, filter via `filterDeals` (import from `./FilterDeals.js`), and per deal `try { await publish(deal, tenant); await markAsSent(deal.id, tenant.id) } catch (err) { console.error('[MonitorDeals] error:', err) }`. No imports from adapters/web/jobs; no pg/axios. Then create `tests/usecases/MonitorDeals.test.ts` with hand-written fake ports (object literals implementing each port; fake publisher pushes to an array; a fake publisher that throws on a chosen deal id) and one `it()` per <behavior> bullet — including an assertion that markAsSent was NOT called for the failed deal and WAS called for the others.
  </action>
  <verify>
    <automated>npx vitest run tests/usecases/MonitorDeals.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/usecases/MonitorDeals.ts` contains `export class MonitorDeals`
    - `grep -v '^//' src/core/usecases/MonitorDeals.ts | grep -E "from '\\.\\./\\.\\./(adapters|web|jobs)'|from 'pg'|from 'axios'" | wc -l` returns 0
    - Test file proves publish-before-markAsSent ordering and per-deal failure isolation
    - `npx vitest run tests/usecases/MonitorDeals.test.ts` exits 0 with at least 5 passing tests
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MonitorDeals is a port-only use case with fake-driven unit tests proving ordering and failure isolation; no god-file modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract SuggestDeals use case with fake-port unit tests</name>
  <read_first>
    - src/web/server.ts lines 575-646 (sendNextSuggestion — the exact rotation + source-variety + dedupe behavior to preserve)
    - docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md lines 252-281 (SuggestDeals reference)
    - src/core/usecases/CategoryRotation.ts (nextCategory / preferDifferentSource)
    - src/core/ports/RotationStore.ts, DealRepository.ts, DealPublisher.ts
  </read_first>
  <behavior>
    - execute(tenant, pool) excludes deals already in findRecentlySentIds(tenant.id, 7); returns early (no publish, no cursor save) when nothing is available.
    - Loads the cursor from rotationStore.load(tenant.id), walks categories via nextCategory until a category with available deals is found, falling back to any available deal.
    - Applies preferDifferentSource against cursor.lastSource so the same source is not repeated when an alternative exists.
    - On successful publish: calls markAsSent(deal.id, tenant.id) and rotationStore.save with the advanced cursor (roundRobinIndex incremented, lastSource set to the chosen deal's marketplace).
    - If publish throws, neither markAsSent nor the cursor save happens (cursor only advances on a real send).
  </behavior>
  <action>
    Create `src/core/usecases/SuggestDeals.ts` exporting `export class SuggestDeals` with constructor injection of `RotationStore`, `DealRepository`, `DealPublisher`, `TenantRepository` (`import type` from `../ports/*.js`) and `async execute(tenant: Tenant, pool: Deal[]): Promise<void>`. Reproduce sendNextSuggestion's domain logic using the pure helpers: dedupe via `findRecentlySentIds(tenant.id, 7)`, load cursor, select category via `nextCategory` (loop until a non-empty category pool or fallback to any available), apply `preferDifferentSource(candidates, cursor.lastSource)`, random pick within the chosen pool (random stays in the use case, not in the pure helper), then on success `publish` → `markAsSent` → `rotationStore.save(tenant.id, { roundRobinIndex: <advanced>, lastSource: deal.marketplace })`. The claimAutoSendSlot timing-lock stays OUT of this use case (it is a job/lock concern, wired in Wave 3). Then create `tests/usecases/SuggestDeals.test.ts` with fake ports + a fake RotationStore (an in-memory object or the real InMemoryRotationStore) and one `it()` per <behavior> bullet, including an assertion that the cursor is NOT saved when publish throws.
  </action>
  <verify>
    <automated>npx vitest run tests/usecases/SuggestDeals.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/usecases/SuggestDeals.ts` contains `export class SuggestDeals`
    - File imports `nextCategory` and/or `preferDifferentSource` from `./CategoryRotation.js`
    - `grep -v '^//' src/core/usecases/SuggestDeals.ts | grep -E "from '\\.\\./\\.\\./(adapters|web|jobs)'|from 'pg'|claimAutoSendSlot" | wc -l` returns 0
    - Test proves cursor advances only on successful publish
    - `npx vitest run tests/usecases/SuggestDeals.test.ts` exits 0 with at least 5 passing tests
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SuggestDeals is a port-only use case reproducing today's rotation/variety/dedupe behavior, with fake-driven tests proving cursor-on-success semantics; server.ts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped Deal[] → use case | Untrusted deal data flows through filtering/selection before publish |
| use case → ports | Use case never touches network/DB directly; all side effects go through injected ports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Repudiation/Integrity | duplicate publish on crash between publish and markAsSent | accept | At-least-once is the deliberate spec choice (duplicate is preferable to dropping a deal); documented in spec lines 224-236 |
| T-03-07 | Denial of Service | one failing deal aborts the whole tenant batch | mitigate | Per-deal try/catch in both use cases isolates failures (asserted by tests) |
| T-03-08 | Tampering | use case importing an adapter and leaking infra into core | mitigate | grep gate forbids adapters/web/jobs/pg/axios imports in core files |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; any future install requires Package Legitimacy Gate + blocking human checkpoint |
</threat_model>

<verification>
- `npx vitest run tests/usecases/MonitorDeals.test.ts tests/usecases/SuggestDeals.test.ts` — both pass with fakes only.
- `npx tsc --noEmit` exits 0.
- Core purity: `grep -rE "from '\\.\\./\\.\\./(adapters|web|jobs)'" src/core/usecases/ | wc -l` returns 0.
- No god-file modified by this plan.
</verification>

<success_criteria>
MonitorDeals and SuggestDeals exist as port-only use cases in `src/core/usecases/`, consume the Wave 1 pure helpers, and pass unit tests driven entirely by fake ports (no network/DB/browser) — preserving today's ordering, dedupe, rotation, and source-variety behavior.
</success_criteria>

<output>
Create `.planning/phases/03-core-domain-extraction/03-03-SUMMARY.md` when done.
</output>
