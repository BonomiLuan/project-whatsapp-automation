---
phase: 03-core-domain-extraction
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - src/index.ts
  - src/web/server.ts
  - src/jobs/monitorPelando.ts
  - src/jobs/monitorML.ts
  - src/jobs/cronLock.ts
  - tests/wiring/composition.test.ts
autonomous: false
requirements: [EXTRACT-02, EXTRACT-08]
must_haves:
  truths:
    - "src/index.ts instantiates the adapters, builds the use cases (MonitorDeals, SuggestDeals), and injects them into the jobs — it is the single composition root"
    - "Jobs receive their dependencies via DI (Lock + use case) instead of importing business functions directly from server.ts"
    - "server.ts no longer contains the inline rotation/suggestion business logic (sendNextSuggestion's rotation, roundRobinIndex/lastSentSource) — that logic now lives in SuggestDeals; server.ts keeps only HTTP wiring + cache"
    - "tsc --noEmit passes and Railway stays deployable after the change"
  artifacts:
    - path: "src/index.ts"
      provides: "Composition root with manual DI wiring"
      contains: "new MonitorDeals"
    - path: "src/web/server.ts"
      provides: "HTTP/Express wiring with business logic removed"
    - path: "tests/wiring/composition.test.ts"
      provides: "Smoke test that the composition root wires without throwing"
  key_links:
    - from: "src/index.ts"
      to: "src/core/usecases/MonitorDeals.ts"
      via: "new MonitorDeals(...)"
      pattern: "new MonitorDeals"
    - from: "src/jobs/monitorPelando.ts"
      to: "Lock + MonitorDeals"
      via: "DI registration function"
      pattern: "register|MonitorDeals|Lock"
---

<objective>
Close the phase by wiring everything together in the composition root and reducing the god-files to wiring (EXTRACT-08): `src/index.ts` instantiates the Wave 1/2 adapters, constructs `MonitorDeals` and `SuggestDeals`, and registers the jobs with their injected dependencies; `src/jobs/*` switch from importing business functions out of `server.ts` to receiving `Lock` + use case via DI (D-04c); and `server.ts` loses its inline suggestion/rotation business logic (EXTRACT-02) now that `SuggestDeals` owns it. This is the only plan that touches the god-files, and it depends on every prior plan in the phase.

Purpose: EXTRACT-08 (god-files = wiring), EXTRACT-02 (server.ts characterized then reduced), D-04c (jobs use Lock port via DI), and the spec's composition-root design (lines 456-486).
Output: rewired index.ts + jobs + a reduced server.ts + a composition smoke test. Includes a human-verify checkpoint because production behavior (cron sends, Telegram delivery) cannot be fully proven by tsc/unit tests alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-domain-extraction/03-CONTEXT.md
@.planning/phases/03-core-domain-extraction/03-PATTERNS.md
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md

@src/index.ts
@src/web/server.ts
@src/jobs/monitorPelando.ts
@src/jobs/monitorML.ts
@src/jobs/cronLock.ts

# Prior-plan outputs being wired
@src/core/usecases/MonitorDeals.ts
@src/core/usecases/SuggestDeals.ts
@src/adapters/lock/PgAdvisoryLock.ts
@src/adapters/store/InMemoryRotationStore.ts
@src/adapters/db/PgDealRepository.ts
@src/adapters/db/PgTenantRepository.ts
@src/adapters/publishers/TelegramPublisher.ts
@src/adapters/scrapers/PelandoScraper.ts
@src/adapters/scrapers/MercadoLivreScraper.ts

<interfaces>
From src/adapters/scheduler/NodeCronScheduler.ts: implements Scheduler { schedule(name, cron, job) }
From src/core/ports/Lock.ts: withLock<T>(key, fn): Promise<T | null>
Use case constructors (from 03-03):
  new MonitorDeals(scraper, publisher, dealRepo, tenantRepo)
  new SuggestDeals(rotationStore, dealRepo, publisher, tenantRepo)
Current cron schedule (must be preserved):
  monitorPelando: '*/30 * * * *'   (lock 111222333 → key 'monitor:pelando')
  refreshDeals:   '*/30 * * * *'
  sendNextSuggestion: '*/15 7-22 * * *'  (claimAutoSendSlot(13))
  resetDailyState: '0 0 * * *'
  timezone: 'America/Sao_Paulo'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert jobs to DI registration functions</name>
  <files>src/jobs/monitorPelando.ts, src/jobs/monitorML.ts, src/jobs/cronLock.ts</files>
  <read_first>
    - src/jobs/monitorPelando.ts, src/jobs/monitorML.ts, src/jobs/cronLock.ts (current: each imports a function from server.ts and calls cron.schedule directly)
    - src/adapters/scheduler/NodeCronScheduler.ts (Scheduler.schedule signature)
    - src/core/ports/Lock.ts
    - src/core/usecases/MonitorDeals.ts, SuggestDeals.ts (execute signatures)
    - docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md lines 527-538 (jobs/ wiring example: register(scheduler, lock, useCase))
  </read_first>
  <action>
    Rewrite each job file to export a `register...` function instead of running `cron.schedule` at import time. `src/jobs/monitorPelando.ts` → `export function registerPelandoMonitor(scheduler: Scheduler, lock: Lock, monitor: MonitorDeals)` that schedules `'*/30 * * * *'` (timezone America/Sao_Paulo) to run `() => lock.withLock('monitor:pelando', () => monitor.execute())` — replacing the old direct `withCronLock(111222333)` usage (D-04c). `src/jobs/monitorML.ts` → `export function registerMLMonitor(scheduler, lock, monitor)` scheduling `'*/30 * * * *'` for the ML monitor execute (key 'monitor:ml'). `src/jobs/cronLock.ts` → `export function registerSuggestionJobs(scheduler, suggest: SuggestDeals, rotationStore, ...)` scheduling `'*/15 7-22 * * *'` for the suggestion send and `'0 0 * * *'` for the daily reset (calling the reset path / rotationStore.reset). Import `Scheduler` from `../core/ports/Scheduler.js`, `Lock` from `../core/ports/Lock.js`, and the use cases from `../core/usecases/*.js`. No `cron.schedule` call may remain at module top level in any job file — scheduling happens only inside the exported register functions, invoked by index.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit; test $(grep -rl "^cron.schedule\|^import cron" src/jobs/ | wc -l) -eq 0 && echo NO_TOPLEVEL_CRON</automated>
  </verify>
  <acceptance_criteria>
    - Each job file exports a `register*` function and contains no top-level `cron.schedule(` call (scheduling is inside the exported function)
    - `src/jobs/monitorPelando.ts` uses `lock.withLock('monitor:pelando'` (not the raw integer 111222333)
    - Job files import from `../core/ports/*` and `../core/usecases/*` (DI), not business functions from `../web/server.js`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three job files are pure DI registration functions taking Scheduler + Lock + use case, with no import-time side effects and the original cron schedules preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Build composition root in index.ts and strip suggestion logic from server.ts</name>
  <files>src/index.ts, src/web/server.ts, tests/wiring/composition.test.ts</files>
  <read_first>
    - src/index.ts (current 4-line barrel)
    - src/web/server.ts (full — esp. lines 575-646 sendNextSuggestion/rotation state, line 648-658 app.listen bootstrap, and the cron-targeted exports refreshDeals/monitorPelando/monitorML/resetDailyState)
    - docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md lines 456-486 (composition root example)
    - All Wave 1/2 adapter + use case files listed in <context>
  </read_first>
  <action>
    Rewrite `src/index.ts` as the composition root per the spec: instantiate `new PgAdvisoryLock()`, `new InMemoryRotationStore()`, `new PgDealRepository()`, `new PgTenantRepository()`, `new NodeCronScheduler()`, `new TelegramPublisher(...)`, `new PelandoScraper()`, `new MercadoLivreScraper()`; build `const monitorPelandoUseCase = new MonitorDeals(pelandoScraper, telegramPublisher, dealRepo, tenantRepo)` and the ML equivalent, and `const suggest = new SuggestDeals(rotationStore, dealRepo, telegramPublisher, tenantRepo)`; then call `registerPelandoMonitor(scheduler, lock, monitorPelandoUseCase)`, `registerMLMonitor(...)`, and `registerSuggestionJobs(scheduler, suggest, rotationStore)`. Keep starting the web server and bot (import the server bootstrap and `createBot()` as today) so HTTP + Telegram still come up. In `src/web/server.ts`: REMOVE the inline suggestion business logic — delete the body of `sendNextSuggestion` rotation/selection (lines ~593-646), the module-level `roundRobinIndex`/`lastSentSource` rotation state (lines 577-579), and the inline category-walk; that behavior now lives in `SuggestDeals`. server.ts retains: Express app, routes, `dealsCache`/`refreshDeals` working-set cache (stays in-memory per spec), image cache, auth, and the `app.listen` bootstrap. Where server.ts previously exported `sendNextSuggestion`/`resetDailyState`/`monitorPelando`/`monitorML` for the jobs, replace those exports with thin shims OR remove them now that index.ts drives the use cases via the jobs — ensure nothing still imports a now-deleted export (fix all references). Do NOT remove `getCachedDeals`/`refreshDeals` if other code still needs the working-set cache that feeds SuggestDeals' `pool`. Finally create `tests/wiring/composition.test.ts`: a smoke test that imports the register functions and constructs the use cases with fake/real adapters and asserts wiring does not throw (no live cron firing — pass a fake Scheduler whose schedule() records calls).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/wiring/composition.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.ts` contains `new MonitorDeals` and `new SuggestDeals` and calls the three `register*` functions
    - `grep -c "roundRobinIndex\|lastSentSource" src/web/server.ts` returns 0 (rotation state moved out)
    - `grep -c "ALL_CATEGORIES\[" src/web/server.ts` returns 0 (inline category-walk removed)
    - No file imports a server.ts export that was deleted (`npx tsc --noEmit` exits 0 proves this)
    - `tests/wiring/composition.test.ts` passes with a fake Scheduler recording scheduled job names
    - `npx vitest run` (full suite) exits 0 — all prior characterization/unit tests still green
  </acceptance_criteria>
  <done>index.ts is the single composition root wiring adapters→use cases→jobs; server.ts holds only HTTP wiring + caches with the suggestion/rotation business logic removed; full test suite green; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify production behavior unchanged after rewiring</name>
  <files>src/index.ts, src/web/server.ts</files>
  <action>
    Manual verification after Tasks 1-2 complete. No code changes in this task — confirm the running application behaves identically to before the rewiring (boot, cron monitor cycle, correctly-formatted suggestion send, dedupe across restart).
  </action>
  <what-built>
    The composition root now drives the cron jobs through MonitorDeals/SuggestDeals use cases via DI, and server.ts no longer contains inline suggestion/rotation logic. tsc passes and the full Vitest suite (characterization + unit + composition smoke) is green.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` locally (or deploy the branch to the Railway preview).
    2. Confirm the server boots without errors and logs `✅ Servidor rodando` + bot startup.
    3. Confirm the Pelando monitor cron path runs: watch for `[pelando:monitor]` logs on the `*/30` schedule (or trigger the registered job manually if a manual hook exists).
    4. Confirm a suggestion send still produces a correctly formatted Telegram message (same caption layout as before: emoji + title, price line, buy link) and that the same source is not sent twice in a row.
    5. Confirm no deal is sent twice within the dedupe window after a restart.
  </how-to-verify>
  <resume-signal>Type "approved" once boot + a monitor cycle + a correctly-formatted suggestion send are observed, or describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.ts → process env | DATABASE_URL, TELEGRAM_BOT_TOKEN read at composition time |
| cron → Lock → Postgres | Scheduled jobs serialized across replicas via advisory lock |
| Express routes → handlers | HTTP input crosses into server.ts (unchanged auth boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Denial of Service | rewiring changes cron schedules/locks → duplicate or skipped sends | mitigate | Preserve exact cron strings + timezone; route monitor through lock.withLock('monitor:pelando'); human-verify checkpoint observes a real cycle |
| T-03-13 | Integrity | a deleted server.ts export still referenced → runtime crash at boot | mitigate | tsc --noEmit gate catches dangling imports; composition smoke test constructs the graph without throwing |
| T-03-14 | Elevation of Privilege | dashboard auth accidentally removed during server.ts reduction | mitigate | Only suggestion/rotation logic is removed; auth middleware (isAuthenticated, /api guard) explicitly retained; acceptance criteria scope deletions to rotation state only |
| T-03-15 | Tampering | composition root logging secrets | mitigate | Do not console.log env values; instantiate adapters with process.env reads only |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; any future install requires Package Legitimacy Gate + blocking human checkpoint |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after rewiring.
- `npx vitest run` — entire suite (links, routes, usecases/*, adapters/*, wiring) passes.
- `grep -rE "roundRobinIndex|lastSentSource|ALL_CATEGORIES\[" src/web/server.ts | wc -l` returns 0 (business logic removed).
- No job file runs `cron.schedule` at import time.
- Human-verify checkpoint approved: boot + monitor cycle + correctly-formatted suggestion send observed in the running app.
</verification>

<success_criteria>
index.ts is the single composition root; jobs are DI register functions using the Lock port + use cases; server.ts is reduced to HTTP wiring + caches with the suggestion/rotation business logic gone; the full test suite is green; tsc is clean; and a human confirmed production behavior is unchanged in a running instance. This satisfies EXTRACT-02 and EXTRACT-08, completing the phase.
</success_criteria>

<output>
Create `.planning/phases/03-core-domain-extraction/03-05-SUMMARY.md` when done.
</output>
