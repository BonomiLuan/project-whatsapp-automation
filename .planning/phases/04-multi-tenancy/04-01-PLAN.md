---
phase: 04-multi-tenancy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/001_initial_schema.ts
  - migrations/002_seed_default_tenant.ts
  - package.json
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03

must_haves:
  truths:
    - "Tabelas tenants, deal_history, rotation_state, deal_feedback existem no banco após migrations rodar"
    - "Coluna tenant_id em deal_history tem DEFAULT 'default' — linhas antigas são preservadas"
    - "Tenant 'default' existe na tabela tenants após migration 002 rodar"
    - "pgmigrations table rastreia quais migrations foram aplicadas"
  artifacts:
    - path: "migrations/001_initial_schema.ts"
      provides: "DDL para tenants, deal_history, rotation_state, deal_feedback"
      contains: "pgm.createTable"
    - path: "migrations/002_seed_default_tenant.ts"
      provides: "Upsert do tenant 'default' com channels do env TELEGRAM_CHAT_IDS"
      contains: "INSERT INTO tenants"
    - path: "package.json"
      provides: "node-pg-migrate listado em dependencies"
      contains: "node-pg-migrate"
  key_links:
    - from: "migrations/001_initial_schema.ts"
      to: "deal_history.tenant_id"
      via: "pgm.addColumns com ifNotExists e DEFAULT 'default'"
      pattern: "tenant_id.*default"
    - from: "migrations/002_seed_default_tenant.ts"
      to: "tenants table"
      via: "INSERT ... ON CONFLICT DO NOTHING"
      pattern: "ON CONFLICT.*DO NOTHING"
---

<objective>
Instalar node-pg-migrate e criar os dois primeiros migration files que ativam o schema multi-tenant.

Migration 001 cria as quatro tabelas (tenants, deal_history com coluna tenant_id, rotation_state, deal_feedback) de forma idempotente. Migration 002 faz upsert do tenant 'default' com os channels lidos de TELEGRAM_CHAT_IDS.

Purpose: Sem essas tabelas o PgDealRepository, PgTenantRepository e PgRotationStore falharão em produção. As migrations bloqueiam o processo antes de qualquer wiring.

Output: migrations/001_initial_schema.ts, migrations/002_seed_default_tenant.ts, package.json atualizado com node-pg-migrate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-tenancy/04-RESEARCH.md
</context>

<interfaces>
<!-- Contratos e padrões que o executor deve seguir. -->

Port interfaces relevantes (lidos de src/core/ports/):

RotationStore:
  load(tenantId: string): Promise<RotationCursor>
  save(tenantId: string, cursor: RotationCursor): Promise<void>
  reset(tenantId: string): Promise<void>
  RotationCursor = { roundRobinIndex: number; lastSource: string | null }

Domain Tenant:
  id: string, name: string, channels: Channel[], filters: Filter,
  affiliates: AffiliateConfig, active: boolean

node-pg-migrate v8 migration file shape (ASSUMED — verificar README v8):
  import type { MigrationBuilder, ColumnDefinitions } from 'node-pg-migrate'
  export const shorthands: ColumnDefinitions | undefined = undefined
  export async function up(pgm: MigrationBuilder): Promise<void>
  export async function down(pgm: MigrationBuilder): Promise<void>

DDL Schema alvo (de 04-RESEARCH.md Schema DDL Reference):
  tenants: id TEXT PK, name TEXT NOT NULL, active BOOLEAN DEFAULT true,
           channels JSONB DEFAULT '[]', filters JSONB DEFAULT '{"minDiscount":0,...}',
           affiliates JSONB DEFAULT '{}'
  deal_history: deal_id TEXT, tenant_id TEXT DEFAULT 'default', sent_at TIMESTAMPTZ DEFAULT NOW()
               PRIMARY KEY (deal_id, tenant_id)
  rotation_state: tenant_id TEXT PK, round_robin_index INTEGER DEFAULT 0,
                  last_source TEXT, updated_at TIMESTAMPTZ DEFAULT NOW()
  deal_feedback: id BIGSERIAL PK, deal_id TEXT, tenant_id TEXT, user_id TEXT,
                 reaction TEXT CHECK (reaction IN ('like','dislike')), created_at TIMESTAMPTZ,
                 UNIQUE (deal_id, tenant_id, user_id)
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Verificar legitimidade do node-pg-migrate antes de instalar</name>
  <what-built>
    node-pg-migrate@8.0.4 está listado no Package Legitimacy Audit da RESEARCH.md como [ASSUMED] (slopcheck indisponível no sandbox de pesquisa). Antes de instalar, confirme manualmente:
    1. Acesse https://www.npmjs.com/package/node-pg-migrate — confirme que o pacote existe e o publisher é "salsita"
    2. Acesse https://github.com/salsita/node-pg-migrate — confirme que o repositório é da org salsita e tem atividade recente
    3. Acesse https://www.npmjs.com/package/node-pg-migrate?activeTab=versions — confirme que a versão 8.0.4 existe publicada
  </what-built>
  <how-to-verify>
    Após checar os três links acima:
    - npm: publisher = "salsita", weekly downloads > 50k, última versão recente
    - GitHub: org salsita, estrelas visíveis, commits recentes (2025 ou 2026)
    - Versão 8.0.4 aparece na lista de versões publicadas
  </how-to-verify>
  <resume-signal>Type "approved" se os três checks passaram, ou descreva o problema encontrado</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Instalar node-pg-migrate e criar migration 001_initial_schema</name>
  <files>package.json, migrations/001_initial_schema.ts</files>

  <read_first>
    - package.json — para adicionar node-pg-migrate em dependencies na posição correta
    - src/adapters/db/PgDealRepository.ts — verificar o SQL exato que ele usa para deal_history (tenant_id, deal_id) e confirmar a forma de PRIMARY KEY esperada
    - src/adapters/db/PgTenantRepository.ts — verificar colunas esperadas para a tabela tenants e o formato JSONB de channels/filters/affiliates
  </read_first>

  <action>
    1. Rodar: npm install node-pg-migrate — isso adiciona node-pg-migrate@8.0.4 em package.json dependencies.

    2. Criar pasta migrations/ na raiz do projeto (mesmo nível de src/, package.json).

    3. Criar migrations/001_initial_schema.ts com as seguintes operações em up():
       - pgm.createTable('tenants', { id: { type: 'text', primaryKey: true }, name: { type: 'text', notNull: true }, active: { type: 'boolean', notNull: true, default: true }, channels: { type: 'jsonb', notNull: true, default: "'[]'" }, filters: { type: 'jsonb', notNull: true, default: "'{\"minDiscount\":0,\"categories\":[],\"keywords\":[],\"excludeKeywords\":[]}'" }, affiliates: { type: 'jsonb', notNull: true, default: "'{}'" } }, { ifNotExists: true })
       - pgm.createTable('deal_history', { deal_id: { type: 'text', notNull: true }, tenant_id: { type: 'text', notNull: true, default: "'default'" }, sent_at: { type: 'timestamptz', notNull: true, default: pgm.func('NOW()') } }, { ifNotExists: true, constraints: { primaryKey: ['deal_id', 'tenant_id'] } })
         Se a tabela deal_history já existir (pode ter sido criada por versão anterior), adicionar coluna tenant_id com: pgm.addColumns('deal_history', { tenant_id: { type: 'text', notNull: true, default: "'default'" } }, { ifNotExists: true })
       - pgm.createTable('rotation_state', { tenant_id: { type: 'text', primaryKey: true }, round_robin_index: { type: 'integer', notNull: true, default: 0 }, last_source: { type: 'text' }, updated_at: { type: 'timestamptz', notNull: true, default: pgm.func('NOW()') } }, { ifNotExists: true })
       - pgm.createTable('deal_feedback', { id: { type: 'bigserial', primaryKey: true }, deal_id: { type: 'text', notNull: true }, tenant_id: { type: 'text', notNull: true }, user_id: { type: 'text', notNull: true }, reaction: { type: 'text', notNull: true }, created_at: { type: 'timestamptz', notNull: true, default: pgm.func('NOW()') } }, { ifNotExists: true, constraints: { unique: ['deal_id', 'tenant_id', 'user_id'], check: "reaction IN ('like', 'dislike')" } })

    4. Em down() fazer apenas: pgm.dropTable('deal_feedback', { ifExists: true }); pgm.dropTable('rotation_state', { ifExists: true })
       — NÃO dropar tenants ou deal_history em down (destrutivo para dados existentes).

    5. Usar path.resolve(process.cwd(), 'migrations') como dir no runner (ver Task 2 do plano 04-04) para evitar Pitfall 2.

    Pitfall 1 (CRÍTICO): deal_history pode ter linhas existentes. Nunca usar ADD COLUMN tenant_id TEXT NOT NULL sem DEFAULT — isso falha com "column contains null values". Sempre incluir default: "'default'" na definição da coluna.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - migrations/ existe na raiz com o arquivo 001_initial_schema.ts
    - 001_initial_schema.ts exporta shorthands (undefined), up e down como funções async
    - up() chama pgm.createTable para tenants, deal_history, rotation_state, deal_feedback — todos com ifNotExists: true
    - deal_history cria coluna tenant_id com default: "'default'" e PRIMARY KEY composta (deal_id, tenant_id)
    - down() não dropa tenants ou deal_history
    - package.json contém "node-pg-migrate" em dependencies
    - tsc --noEmit passa sem erros (ou com zero erros novos introduzidos por esta task)
    - NOTA: validação de que as migrations realmente criam as tabelas no banco requer conexão PostgreSQL real e é diferida para o checkpoint humano (Task 3 de 04-04). tsc --noEmit valida apenas tipos e sintaxe dos arquivos de migration em processo.
  </acceptance_criteria>

  <done>migrations/001_initial_schema.ts criado com DDL para as 4 tabelas; node-pg-migrate em package.json</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 002_seed_default_tenant</name>
  <files>migrations/002_seed_default_tenant.ts</files>

  <read_first>
    - scripts/seed-default-tenant.ts — lógica atual de seed (lê TELEGRAM_CHAT_IDS, formata channels, faz upsert em tenants) para replicar exatamente a mesma lógica no migration
    - src/core/domain/Tenant.ts — estrutura do tipo Tenant (id, name, channels, filters, affiliates, active) para montar o JSONB correto
    - migrations/001_initial_schema.ts — confirmar que tabela tenants já existe antes do seed
  </read_first>

  <action>
    Criar migrations/002_seed_default_tenant.ts com up() que:
    1. Lê process.env.TELEGRAM_CHAT_IDS (pode ser undefined em staging) — se ausente, usa '[]' para channels.
    2. Parseia TELEGRAM_CHAT_IDS como string separada por vírgula e monta array de Channel objects: [{ type: 'telegram', channelId: id.trim() }].
    3. Executa SQL via pgm.sql() (ou db.query() que o MigrationBuilder expõe):
       INSERT INTO tenants (id, name, active, channels, filters, affiliates)
       VALUES ('default', 'Default', true, $channels_json, $filters_json, '{}')
       ON CONFLICT (id) DO NOTHING
       onde $channels_json = JSON.stringify(channelsArray) e $filters_json = JSON.stringify({ minDiscount: 0, categories: [], keywords: [], excludeKeywords: [] }).
    4. Logar no console: '[migration 002] default tenant upserted or already exists'.
    down(): no-op (não remover tenant existente).

    NOTA: node-pg-migrate v8 expõe pgm.sql(string) para raw SQL. Se a assinatura exata de pgm.sql diferir, verificar o README oficial em node_modules/node-pg-migrate/README.md antes de implementar.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - migrations/002_seed_default_tenant.ts existe e exporta up e down
    - up() contém INSERT INTO tenants ... ON CONFLICT (id) DO NOTHING
    - O valor de channels no INSERT é montado a partir de process.env.TELEGRAM_CHAT_IDS (não hardcoded)
    - O valor de filters no INSERT inclui minDiscount, categories, keywords, excludeKeywords
    - down() é no-op (função vazia ou sem operações)
    - tsc --noEmit passa sem erros novos
  </acceptance_criteria>

  <done>migrations/002_seed_default_tenant.ts criado com upsert idempotente do tenant 'default'</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → migration seed | process.env.TELEGRAM_CHAT_IDS entra diretamente como JSONB — não é user input mas requer serialização segura |
| npm install | Instalação de dependência externa node-pg-migrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | deal_history migration — ADD COLUMN sem DEFAULT | mitigate | Sempre usar default: "'default'" na definição — Pitfall 1 da RESEARCH.md |
| T-04-02 | Denial of Service | Migration destrói dados ao fazer DROP TABLE em down() | mitigate | down() não dropa tenants ou deal_history — apenas rotation_state e deal_feedback |
| T-04-SC | Tampering | npm install node-pg-migrate | mitigate | Checkpoint Task 0 (human-verify) valida origin em npmjs.com e github.com/salsita antes de instalar |
</threat_model>

<verification>
- node-pg-migrate aparece em package.json dependencies
- migrations/001_initial_schema.ts e migrations/002_seed_default_tenant.ts existem
- tsc --noEmit passa
- Arquivos de migration exportam up e down (grep -c "export async function up" migrations/*.ts conta 2)
</verification>

<success_criteria>
- node-pg-migrate instalado e em package.json
- 001_initial_schema.ts cria tenants, deal_history (com tenant_id DEFAULT 'default'), rotation_state, deal_feedback — todos com IF NOT EXISTS
- 002_seed_default_tenant.ts faz INSERT ... ON CONFLICT (id) DO NOTHING para tenant 'default'
- tsc --noEmit limpo
</success_criteria>

<output>
Create `.planning/phases/04-multi-tenancy/04-01-SUMMARY.md` when done
</output>
