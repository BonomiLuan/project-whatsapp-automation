---
phase: 04-multi-tenancy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/affiliates/AmazonAffiliate.ts
  - src/adapters/affiliates/MLAffiliate.ts
  - src/adapters/affiliates/ShopeeAffiliate.ts
  - src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts
autonomous: true
requirements:
  - TENANT-07

must_haves:
  truths:
    - "CompositeAffiliateLinkBuilder.supports('amazon') retorna true quando AmazonAffiliateLinkBuilder está no array"
    - "CompositeAffiliateLinkBuilder.build() delega para o adapter correto e retorna deal com URL reescrita"
    - "Passthrough: build() retorna deal inalterado quando nenhum adapter suporta o marketplace"
    - "Wrapper classes implementam AffiliateLinkBuilder (suporte a supports e build)"
  artifacts:
    - path: "src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts"
      provides: "Composite que delega para Amazon/ML/Shopee adapter"
      exports: ["CompositeAffiliateLinkBuilder"]
    - path: "src/adapters/affiliates/AmazonAffiliate.ts"
      provides: "Classe AmazonAffiliateLinkBuilder implementando AffiliateLinkBuilder"
      contains: "class AmazonAffiliateLinkBuilder"
    - path: "src/adapters/affiliates/MLAffiliate.ts"
      provides: "Classe MLAffiliateLinkBuilder implementando AffiliateLinkBuilder"
      contains: "class MLAffiliateLinkBuilder"
    - path: "src/adapters/affiliates/ShopeeAffiliate.ts"
      provides: "Classe ShopeeAffiliateLinkBuilder implementando AffiliateLinkBuilder"
      contains: "class ShopeeAffiliateLinkBuilder"
  key_links:
    - from: "CompositeAffiliateLinkBuilder"
      to: "AmazonAffiliateLinkBuilder / MLAffiliateLinkBuilder / ShopeeAffiliateLinkBuilder"
      via: "adapters.find(a => a.supports(deal.marketplace))"
      pattern: "adapters.find"
---

<objective>
Criar as classes wrapper que adaptam as funções afiliadas existentes para o port AffiliateLinkBuilder, e o CompositeAffiliateLinkBuilder que delega para o adapter correto.

Purpose: SuggestDeals precisa de um AffiliateLinkBuilder injetável. As três funções existentes (injectAmazonTag, injectMLTag, generateAffiliateLink) precisam de wrapper classes que implementem supports() + build().

Output: Três classes wrapper adicionadas aos arquivos existentes + CompositeAffiliateLinkBuilder.ts novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-tenancy/04-RESEARCH.md
</context>

<interfaces>
<!-- Contratos que o executor deve implementar. -->

Port AffiliateLinkBuilder (src/core/ports/AffiliateLinkBuilder.ts):
  interface AffiliateLinkBuilder {
    supports(marketplace: Marketplace): boolean
    build(deal: Deal, config: AffiliateConfig): Promise<Deal>
  }

Domain types:
  Marketplace = 'pelando' | 'mercadolivre' | 'shopee' | 'amazon'
  AffiliateConfig = { amazonTag?: string; mlAppId?: string; shopeeSourceId?: string }
  Deal = { id: string; title: string; url: string; marketplace: Marketplace; ... }

Funções existentes a serem encapsuladas:
  AmazonAffiliate.ts: injectAmazonTag(url: string): Promise<string>; isAmazonUrl(url: string): boolean
  MLAffiliate.ts: injectMLTag(url: string): Promise<string>; isMercadoLivreUrl(url: string): boolean
  ShopeeAffiliate.ts: generateAffiliateLink(originUrl: string, subIds?: SubIds): Promise<string>
    SubIds = { sourceId?: string; appId?: string; mattWord?: string }

CompositeAffiliateLinkBuilder pattern (de 04-RESEARCH.md Pattern 3):
  constructor(private readonly adapters: AffiliateLinkBuilder[])
  supports(marketplace): boolean — delegates to adapters.some(a => a.supports(marketplace))
  build(deal, config): Promise<Deal> — delegates to adapters.find(a => a.supports(deal.marketplace))
    se nenhum adapter suporta, retorna deal inalterado (passthrough)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar wrapper classes nos três arquivos de affiliate adapter</name>
  <files>
    src/adapters/affiliates/AmazonAffiliate.ts,
    src/adapters/affiliates/MLAffiliate.ts,
    src/adapters/affiliates/ShopeeAffiliate.ts
  </files>

  <behavior>
    - AmazonAffiliateLinkBuilder.supports('amazon') = true; supports('shopee') = false
    - AmazonAffiliateLinkBuilder.build(deal, config) chama injectAmazonTag(deal.url) e retorna { ...deal, url: affiliateUrl }
    - MLAffiliateLinkBuilder.supports('mercadolivre') = true; supports('amazon') = false
    - MLAffiliateLinkBuilder.build(deal, config) chama injectMLTag(deal.url) e retorna { ...deal, url: affiliateUrl }
    - ShopeeAffiliateLinkBuilder.supports('shopee') = true; supports('pelando') = false
    - ShopeeAffiliateLinkBuilder.build(deal, config) chama generateAffiliateLink(deal.url, { sourceId: config.shopeeSourceId }) e retorna { ...deal, url: affiliateUrl }
  </behavior>

  <read_first>
    - src/adapters/affiliates/AmazonAffiliate.ts — ver assinatura de injectAmazonTag e isAmazonUrl; não alterar funções existentes
    - src/adapters/affiliates/MLAffiliate.ts — ver assinatura de injectMLTag e isMercadoLivreUrl; não alterar funções existentes
    - src/adapters/affiliates/ShopeeAffiliate.ts — ver assinatura de generateAffiliateLink e tipo SubIds; não alterar funções existentes
    - src/core/ports/AffiliateLinkBuilder.ts — assinatura exata do port a implementar
    - src/core/domain/Deal.ts — tipo Deal (para o spread { ...deal, url })
    - src/core/domain/Tenant.ts — tipo AffiliateConfig (para o parâmetro config do build)
  </read_first>

  <action>
    Para cada arquivo, ACRESCENTAR ao final (não modificar código existente) a classe wrapper:

    Em AmazonAffiliate.ts — adicionar:
    import type { AffiliateLinkBuilder } from '../../core/ports/AffiliateLinkBuilder.js'
    import type { Deal, Marketplace } from '../../core/domain/Deal.js'
    import type { AffiliateConfig } from '../../core/domain/Tenant.js'
    export class AmazonAffiliateLinkBuilder implements AffiliateLinkBuilder {
      supports(marketplace: Marketplace): boolean { return marketplace === 'amazon' }
      async build(deal: Deal, _config: AffiliateConfig): Promise<Deal> {
        const url = await injectAmazonTag(deal.url)
        return { ...deal, url }
      }
    }

    Em MLAffiliate.ts — adicionar:
    import type { AffiliateLinkBuilder } from '../../core/ports/AffiliateLinkBuilder.js'
    import type { Deal, Marketplace } from '../../core/domain/Deal.js'
    import type { AffiliateConfig } from '../../core/domain/Tenant.js'
    export class MLAffiliateLinkBuilder implements AffiliateLinkBuilder {
      supports(marketplace: Marketplace): boolean { return marketplace === 'mercadolivre' }
      async build(deal: Deal, _config: AffiliateConfig): Promise<Deal> {
        const url = await injectMLTag(deal.url)
        return { ...deal, url }
      }
    }

    Em ShopeeAffiliate.ts — adicionar:
    import type { AffiliateLinkBuilder } from '../../core/ports/AffiliateLinkBuilder.js'
    import type { Deal, Marketplace } from '../../core/domain/Deal.js'
    import type { AffiliateConfig } from '../../core/domain/Tenant.js'
    export class ShopeeAffiliateLinkBuilder implements AffiliateLinkBuilder {
      supports(marketplace: Marketplace): boolean { return marketplace === 'shopee' }
      async build(deal: Deal, config: AffiliateConfig): Promise<Deal> {
        const url = await generateAffiliateLink(deal.url, { sourceId: config.shopeeSourceId })
        return { ...deal, url }
      }
    }

    PROIBIDO: não remover ou alterar as funções exportadas existentes (injectAmazonTag, injectMLTag, generateAffiliateLink, etc.) — elas ainda são usadas em outros lugares.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "class AmazonAffiliateLinkBuilder implements AffiliateLinkBuilder" src/adapters/affiliates/AmazonAffiliate.ts retorna 1
    - grep -c "class MLAffiliateLinkBuilder implements AffiliateLinkBuilder" src/adapters/affiliates/MLAffiliate.ts retorna 1
    - grep -c "class ShopeeAffiliateLinkBuilder implements AffiliateLinkBuilder" src/adapters/affiliates/ShopeeAffiliate.ts retorna 1
    - Funções exportadas originais (injectAmazonTag, injectMLTag, generateAffiliateLink) ainda estão presentes e exportadas
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>Três classes wrapper implementando AffiliateLinkBuilder adicionadas aos arquivos existentes sem remover código existente</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar CompositeAffiliateLinkBuilder com testes</name>
  <files>
    src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts,
    tests/adapters/CompositeAffiliateLinkBuilder.test.ts
  </files>

  <behavior>
    - new CompositeAffiliateLinkBuilder([amazonAdapter, mlAdapter]).supports('amazon') = true
    - new CompositeAffiliateLinkBuilder([amazonAdapter]).supports('shopee') = false
    - build() com deal.marketplace='amazon' e amazonAdapter no array: chama amazonAdapter.build() e retorna resultado
    - build() com deal.marketplace='pelando' (nenhum adapter suporta): retorna deal inalterado (passthrough)
    - build() com múltiplos adapters: apenas o primeiro que supports() recebe a chamada
  </behavior>

  <read_first>
    - src/core/ports/AffiliateLinkBuilder.ts — assinatura do port
    - src/core/domain/Deal.ts — tipo Deal e Marketplace
    - src/core/domain/Tenant.ts — tipo AffiliateConfig
    - src/adapters/affiliates/AmazonAffiliate.ts — confirmar que AmazonAffiliateLinkBuilder foi exportado na Task 1
  </read_first>

  <action>
    1. Criar tests/adapters/CompositeAffiliateLinkBuilder.test.ts primeiro (RED):
       - Usar stubs inline implementando AffiliateLinkBuilder: { supports: () => true/false, build: async (deal) => ({...deal, url: 'INJECTED'}) }
       - Testar os 5 behaviors listados acima
       - Rodar: vitest run tests/adapters/CompositeAffiliateLinkBuilder.test.ts — deve FALHAR (arquivo fonte não existe)

    2. Criar src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts (GREEN):
       - Importar: AffiliateLinkBuilder from '../../core/ports/AffiliateLinkBuilder.js'; Deal, Marketplace from domain; AffiliateConfig from domain
       - export class CompositeAffiliateLinkBuilder implements AffiliateLinkBuilder
       - constructor(private readonly adapters: AffiliateLinkBuilder[])
       - supports(marketplace: Marketplace): boolean — return this.adapters.some(a => a.supports(marketplace))
       - async build(deal: Deal, config: AffiliateConfig): Promise<Deal> — const adapter = this.adapters.find(a => a.supports(deal.marketplace)); if (!adapter) return deal; return adapter.build(deal, config)
       - Rodar: vitest run tests/adapters/CompositeAffiliateLinkBuilder.test.ts — deve PASSAR

    3. tsc --noEmit deve passar.
  </action>

  <verify>
    <automated>npx vitest run tests/adapters/CompositeAffiliateLinkBuilder.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/adapters/CompositeAffiliateLinkBuilder.test.ts existe e tem ao menos 5 test cases (supports true, supports false, build delegates, build passthrough, build picks first supporter)
    - vitest run tests/adapters/CompositeAffiliateLinkBuilder.test.ts passa com 0 falhas
    - src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts exporta a classe CompositeAffiliateLinkBuilder
    - A implementação de build() retorna deal inalterado quando nenhum adapter suporta o marketplace (não lança erro)
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>CompositeAffiliateLinkBuilder criado com testes passando; delegation + passthrough verificados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SuggestDeals → CompositeAffiliateLinkBuilder | deal.url reescrito por adapter externo antes de publicar |
| AffiliateConfig → adapters | shopeeSourceId lido do tenant config; não vem de user input direto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | CompositeAffiliateLinkBuilder.build() — URL reescrita | accept | URL vem de deal.url já validado por scraper; risco baixo, sem PII |
| T-04-04 | Spoofing | AffiliateConfig.shopeeSourceId vazio | accept | generateAffiliateLink já trata subIds undefined — passthrough sem tag |
| T-04-05 | Tampering | Modificar funções exportadas existentes acidentalmente | mitigate | Ação proíbe alterar código existente; apenas acrescenta classe ao final do arquivo |
</threat_model>

<verification>
- vitest run tests/adapters/CompositeAffiliateLinkBuilder.test.ts passa
- tsc --noEmit sem erros
- grep verifica exportação das três classes wrapper
- grep verifica que injectAmazonTag, injectMLTag, generateAffiliateLink ainda estão exportados
</verification>

<success_criteria>
- AmazonAffiliateLinkBuilder, MLAffiliateLinkBuilder, ShopeeAffiliateLinkBuilder exportados de seus arquivos respectivos
- CompositeAffiliateLinkBuilder.ts existe com delegation e passthrough
- Testes passam; tsc limpo
- Código existente dos adapters não foi modificado
</success_criteria>

<output>
Create `.planning/phases/04-multi-tenancy/04-02-SUMMARY.md` when done
</output>
