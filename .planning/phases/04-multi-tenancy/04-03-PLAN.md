---
phase: 04-multi-tenancy
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/adapters/store/PgRotationStore.ts
  - tests/adapters/PgRotationStore.test.ts
  - src/core/usecases/SuggestDeals.ts
  - tests/usecases/SuggestDeals.test.ts
autonomous: true
requirements:
  - TENANT-03
  - TENANT-04
  - TENANT-05

must_haves:
  truths:
    - "PgDealRepository.findRecentlySentIds(tenantId, withinDays) retorna apenas deal_ids do tenant especificado — ids de outros tenants não aparecem"
    - "PgRotationStore.load() retorna { roundRobinIndex: 0, lastSource: null } quando não há linha para o tenantId"
    - "PgRotationStore.save() e subsequente load() retornam o cursor salvo (round_robin_index e last_source corretos)"
    - "PgRotationStore.reset() zera o cursor para roundRobinIndex=0, lastSource=null"
    - "SuggestDeals.execute() aceita AffiliateLinkBuilder como 4° parâmetro opcional sem quebrar testes existentes"
    - "SuggestDeals chama affiliateBuilder.build(deal, tenant.affiliates) entre step 4 (deal selecionado) e step 5 (publish)"
  artifacts:
    - path: "src/adapters/store/PgRotationStore.ts"
      provides: "PgRotationStore implementando RotationStore via upsert em rotation_state"
      exports: ["PgRotationStore"]
    - path: "tests/adapters/PgRotationStore.test.ts"
      provides: "Testes unit com fake pool para load, save, reset"
      contains: "describe('PgRotationStore'"
    - path: "src/core/usecases/SuggestDeals.ts"
      provides: "4° parâmetro opcional affiliateBuilder no constructor"
      contains: "affiliateBuilder"
  key_links:
    - from: "PgRotationStore.save()"
      to: "rotation_state table"
      via: "INSERT ... ON CONFLICT (tenant_id) DO UPDATE"
      pattern: "ON CONFLICT.*tenant_id.*DO UPDATE"
    - from: "SuggestDeals.execute()"
      to: "affiliateBuilder.build()"
      via: "chamada condicional entre deal selecionado e publisher.publish()"
      pattern: "affiliateBuilder.*build"
---

<objective>
Criar PgRotationStore (adapter persistido) e atualizar SuggestDeals para aceitar AffiliateLinkBuilder como 4° parâmetro.

Wave 2: depende de 04-01 (tabela rotation_state já existe via migration).

Purpose: PgRotationStore substitui InMemoryRotationStore em produção para que o cursor de rotação sobreviva restarts. SuggestDeals precisa do 4° parâmetro para injetar links de afiliado antes de publicar.

Output: PgRotationStore.ts com testes, SuggestDeals.ts atualizado com AffiliateLinkBuilder opcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-tenancy/04-RESEARCH.md
@.planning/phases/04-multi-tenancy/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos extraídos do codebase. -->

RotationStore port (src/core/ports/RotationStore.ts):
  export type RotationCursor = { roundRobinIndex: number; lastSource: string | null }
  export interface RotationStore {
    load(tenantId: string): Promise<RotationCursor>
    save(tenantId: string, cursor: RotationCursor): Promise<void>
    reset(tenantId: string): Promise<void>
  }

AffiliateLinkBuilder port (src/core/ports/AffiliateLinkBuilder.ts):
  export interface AffiliateLinkBuilder {
    supports(marketplace: Marketplace): boolean
    build(deal: Deal, config: AffiliateConfig): Promise<Deal>
  }

SuggestDeals.ts — constructor atual (linha 20-25):
  export class SuggestDeals {
    constructor(
      private readonly rotationStore: RotationStore,
      private readonly dealRepo: DealRepository,
      private readonly publisher: DealPublisher,
    ) {}
  }

SuggestDeals.execute() — step 5 atual (linha 69):
  await this.publisher.publish(deal, tenant)
  → affiliate build deve ocorrer ANTES deste publish

pool.ts shared pool (src/adapters/db/pool.ts):
  import pool from '../../adapters/db/pool.js'  (default export Pool)

PgTenantRepository.save() — padrão upsert existente:
  INSERT INTO tenants (...) VALUES (...) ON CONFLICT (id) DO UPDATE SET ... (referência de padrão)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar PgRotationStore com testes de unidade</name>
  <files>
    src/adapters/store/PgRotationStore.ts,
    tests/adapters/PgRotationStore.test.ts
  </files>

  <behavior>
    - load('t1') quando tabela vazia retorna { roundRobinIndex: 0, lastSource: null }
    - save('t1', { roundRobinIndex: 3, lastSource: 'amazon' }) e load('t1') = { roundRobinIndex: 3, lastSource: 'amazon' }
    - save() duas vezes para mesmo tenantId: segunda substitui (upsert, não duplica)
    - reset('t1') após save: load('t1') = { roundRobinIndex: 0, lastSource: null }
    - load() e save() com tenantId diferente não interferem entre si
  </behavior>

  <read_first>
    - src/adapters/store/InMemoryRotationStore.ts — estrutura atual para entender o contrato de RotationStore em uso
    - src/adapters/db/PgTenantRepository.ts — ver o padrão de upsert com ON CONFLICT DO UPDATE e como o pool é importado
    - src/adapters/db/pool.ts — confirmar o path correto do import do pool
    - src/core/ports/RotationStore.ts — assinatura exata do port
  </read_first>

  <action>
    1. Criar tests/adapters/PgRotationStore.test.ts primeiro (RED):
       - Usar fake pool: criar objeto { query: vi.fn() } com respostas mockadas via mockResolvedValueOnce({ rows: [...] })
       - Injetar pool via parâmetro no construtor ou usar vi.mock('../../../src/adapters/db/pool.js')
       - Preferir injeção via construtor para facilitar o mock (PgRotationStore aceita pool como parâmetro opcional ou obrigatório nos testes)
       - Testar os 5 behaviors listados acima
       - Rodar: vitest run tests/adapters/PgRotationStore.test.ts — deve FALHAR

    2. Criar src/adapters/store/PgRotationStore.ts (GREEN):
       - import pool from '../db/pool.js' (path relativo de adapters/store/ para adapters/db/)
       - export class PgRotationStore implements RotationStore
       - load(tenantId): SELECT round_robin_index, last_source FROM rotation_state WHERE tenant_id = $1
         se rows.length === 0, retornar { roundRobinIndex: 0, lastSource: null }
         senão retornar { roundRobinIndex: rows[0].round_robin_index, lastSource: rows[0].last_source }
       - save(tenantId, cursor): INSERT INTO rotation_state (tenant_id, round_robin_index, last_source, updated_at)
         VALUES ($1, $2, $3, NOW()) ON CONFLICT (tenant_id) DO UPDATE
         SET round_robin_index = EXCLUDED.round_robin_index, last_source = EXCLUDED.last_source, updated_at = EXCLUDED.updated_at
       - reset(tenantId): chamar this.save(tenantId, { roundRobinIndex: 0, lastSource: null })
       - Rodar: vitest run tests/adapters/PgRotationStore.test.ts — deve PASSAR

    3. tsc --noEmit deve passar.

    NOTA: O path de import do pool de adapters/store/PgRotationStore.ts para adapters/db/pool.ts é '../db/pool.js' (subir 1 nível, entrar em db/).
  </action>

  <verify>
    <automated>npx vitest run tests/adapters/PgRotationStore.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/adapters/PgRotationStore.test.ts existe com ao menos 5 test cases
    - vitest run tests/adapters/PgRotationStore.test.ts passa com 0 falhas
    - src/adapters/store/PgRotationStore.ts exporta a classe PgRotationStore
    - O SQL de save() contém ON CONFLICT (tenant_id) DO UPDATE (grep -c "ON CONFLICT" src/adapters/store/PgRotationStore.ts retorna 1)
    - reset() chama this.save() com { roundRobinIndex: 0, lastSource: null }
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>PgRotationStore implementado com upsert e testes passando para load, save, reset</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar AffiliateLinkBuilder como 4° parâmetro opcional em SuggestDeals</name>
  <files>
    src/core/usecases/SuggestDeals.ts,
    tests/usecases/SuggestDeals.test.ts
  </files>

  <behavior>
    - SuggestDeals construído sem 4° parâmetro: comportamento existente inalterado (testes existentes passam)
    - SuggestDeals construído com affiliateBuilder stub: execute() chama affiliateBuilder.build(deal, tenant.affiliates) antes de publisher.publish()
    - Se affiliateBuilder.supports(deal.marketplace) = false: deal é publicado sem modificação de URL
    - Se affiliateBuilder.supports(deal.marketplace) = true: deal publicado tem URL retornada por build()
    - publisher.publish() e dealRepo.markAsSent() e rotationStore.save() ainda chamados após build()
  </behavior>

  <read_first>
    - src/core/usecases/SuggestDeals.ts — código completo atual para entender onde inserir a chamada ao affiliateBuilder (entre "if (!deal) return" e "await this.publisher.publish")
    - tests/usecases/SuggestDeals.test.ts — todos os test cases existentes para garantir que não quebram com a mudança
    - src/core/ports/AffiliateLinkBuilder.ts — assinatura do port
    - src/core/domain/Tenant.ts — confirmar que Tenant.affiliates é do tipo AffiliateConfig
  </read_first>

  <action>
    1. Atualizar tests/usecases/SuggestDeals.test.ts (antes de alterar o fonte):
       - Adicionar import do tipo AffiliateLinkBuilder
       - Criar stub: const noopAffiliate: AffiliateLinkBuilder = { supports: () => false, build: async (deal) => deal }
       - Criar stub para teste real: const realAffiliate: AffiliateLinkBuilder = { supports: () => true, build: async (deal) => ({ ...deal, url: 'AFFILIATE_URL' }) }
       - Não alterar test cases existentes — apenas adicionar o 4° argumento noopAffiliate nas instanciações existentes de SuggestDeals se necessário (quando o parâmetro for obrigatório — preferir opcional)
       - Adicionar 2 novos test cases: "chama affiliateBuilder.build quando supports retorna true" e "não modifica deal quando supports retorna false"
       - Rodar: vitest run tests/usecases/SuggestDeals.test.ts — todos os existentes devem continuar passando, novos podem falhar (RED)

    2. Atualizar src/core/usecases/SuggestDeals.ts:
       - Adicionar import de AffiliateLinkBuilder do path '../../core/ports/AffiliateLinkBuilder.js' (mas como SuggestDeals já está em core/usecases/, o import é '../ports/AffiliateLinkBuilder.js')
       - Adicionar no constructor: private readonly affiliateBuilder?: AffiliateLinkBuilder (4° parâmetro, opcional com ?)
       - Entre "if (!deal) return" (linha 65) e "await this.publisher.publish(deal, tenant)" (linha 69), inserir:
         if (this.affiliateBuilder && this.affiliateBuilder.supports(deal.marketplace)) {
           deal = await this.affiliateBuilder.build(deal, tenant.affiliates)
         }
       - Rodar: vitest run tests/usecases/SuggestDeals.test.ts — todos devem PASSAR

    3. tsc --noEmit deve passar.

    CRÍTICO: Parâmetro deve ser OPCIONAL (?) para que instanciações existentes em src/index.ts (linha 36: new SuggestDeals(rotationStore, dealRepo, telegramPublisher)) não precisem ser alteradas neste plano — a composição root é atualizada no plano 04-04.
  </action>

  <verify>
    <automated>npx vitest run tests/usecases/SuggestDeals.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - vitest run tests/usecases/SuggestDeals.test.ts passa com 0 falhas (incluindo todos os testes existentes)
    - grep -c "affiliateBuilder" src/core/usecases/SuggestDeals.ts retorna >= 3 (constructor, if check, build call)
    - O constructor de SuggestDeals tem 4° parâmetro com ? (opcional): private readonly affiliateBuilder?: AffiliateLinkBuilder
    - A chamada affiliateBuilder.build() ocorre ANTES de publisher.publish() e DEPOIS de "if (!deal) return"
    - Nenhum import de adapters/ foi adicionado a SuggestDeals.ts (core não importa adapters)
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>SuggestDeals aceita AffiliateLinkBuilder como 4° parâmetro opcional; testes existentes e novos passando</done>
</task>


<task type="auto" tdd="true">
  <name>Task 3: Testar PgDealRepository.findRecentlySentIds com isolamento por tenant (TENANT-03)</name>
  <files>
    tests/adapters/PgDealRepository.test.ts
  </files>

  <behavior>
    - findRecentlySentIds('tenantA', 7) retorna apenas deal_ids onde tenant_id = 'tenantA' na deal_history
    - findRecentlySentIds('tenantB', 7) NÃO retorna deal_ids inseridos para 'tenantA' (isolamento de tenant)
    - findRecentlySentIds('tenantA', 1) respeita o filtro withinDays — deal com sent_at > 1 dia atrás NÃO aparece
    - findRecentlySentIds('tenantA', 7) quando não há linhas retorna array vazio []
    - Retorno é string[] de deal_ids (não objetos completos)
  </behavior>

  <read_first>
    - src/adapters/db/PgDealRepository.ts — localizar findRecentlySentIds e extrair o SQL exato (WHERE tenant_id =  AND sent_at > NOW() - INTERVAL) para usar nos mocks do fake pool
    - tests/adapters/PgRotationStore.test.ts (criado na Task 1 deste plano) — ver padrão de fake pool com vi.fn() e mockResolvedValueOnce para reutilizar o mesmo estilo
  </read_first>

  <action>
    1. Criar tests/adapters/PgDealRepository.test.ts (RED):
       - Criar fake pool: const fakePool = { query: vi.fn() }
       - Instanciar PgDealRepository com o fake pool via parâmetro de construtor. Se PgDealRepository não aceitar pool por parâmetro, usar vi.mock('../../../src/adapters/db/pool.js', () => ({ default: fakePool }))
       - Test 1 (retorno correto para tenantA): fakePool.query.mockResolvedValueOnce({ rows: [{ deal_id: 'deal-1' }, { deal_id: 'deal-2' }] }); const ids = await repo.findRecentlySentIds('tenantA', 7); expect(ids).toEqual(['deal-1', 'deal-2'])
       - Test 2 (isolamento de tenant): configurar mock para retornar rows vazias quando chamado com 'tenantB'; expect(await repo.findRecentlySentIds('tenantB', 7)).toEqual([])
       - Test 3 (array vazio quando sem histórico): fakePool.query.mockResolvedValueOnce({ rows: [] }); expect(await repo.findRecentlySentIds('tenantA', 7)).toEqual([])
       - Test 4 (verificar que o SQL usa o tenantId correto): capturar args de fakePool.query e verificar que o primeiro parâmetro () é o tenantId passado — confirma isolamento a nível de query
       - Rodar: vitest run tests/adapters/PgDealRepository.test.ts — deve FALHAR (arquivo não existe ainda)

    2. Verificar se PgDealRepository.findRecentlySentIds já implementa o SQL correto (RESEARCH.md confirma que sim: "PgDealRepository already has the correct SQL"). Apenas adicionar suporte a injeção de pool para testes se necessário (parâmetro opcional no constructor, com fallback para import pool.ts).

    3. Rodar: vitest run tests/adapters/PgDealRepository.test.ts — deve PASSAR.

    NOTA: O objetivo desta task é TESTAR o método existente com fake pool e verificar isolamento de tenant, não reimplementar o SQL. Se PgDealRepository.findRecentlySentIds não aceitar pool injetável, usar vi.mock para o módulo pool.ts.
  </action>

  <verify>
    <automated>npx vitest run tests/adapters/PgDealRepository.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/adapters/PgDealRepository.test.ts existe com ao menos 4 test cases cobrindo os behaviors acima
    - vitest run tests/adapters/PgDealRepository.test.ts passa com 0 falhas
    - Os testes verificam que deals de tenantA NÃO aparecem em resultado de tenantB (isolamento explícito)
    - Os testes verificam que o parâmetro  do SQL é o tenantId correto (ou confirmam via comportamento do mock)
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>findRecentlySentIds(tenantId, withinDays) testado com fake pool; isolamento por tenant verificado explicitamente</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rotation_state table → PgRotationStore.load() | Dado lido do banco; round_robin_index pode ser NULL se linha corrompida |
| affiliateBuilder.build() → deal.url | URL reescrita por adapter externo antes de publicar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | PgRotationStore.save() — SQL injection via tenantId | accept | tenantId vem de Tenant.id controlado internamente; não é user input direto |
| T-04-07 | Denial of Service | rotation_state upsert race com dois processos | accept | Phase 4 é single-tenant MVP; PgAdvisoryLock já protege jobs de monitoramento; risco documentado em pitfall 5 da RESEARCH |
| T-04-08 | Tampering | SuggestDeals publica deal com URL não-afiliada se supports() falha | accept | Passthrough é comportamento correto e esperado para marketplaces sem tag |
</threat_model>

<verification>
- vitest run tests/adapters/PgRotationStore.test.ts passa
- vitest run tests/usecases/SuggestDeals.test.ts passa
- tsc --noEmit limpo
- grep -c "ON CONFLICT" src/adapters/store/PgRotationStore.ts = 1
- grep -c "affiliateBuilder" src/core/usecases/SuggestDeals.ts >= 3
</verification>

<success_criteria>
- PgRotationStore implementado com load/save/reset e upsert correto
- Testes PgRotationStore passam com fake pool
- SuggestDeals aceita AffiliateLinkBuilder opcional sem quebrar testes existentes
- affiliate build inserido entre deal selecionado e publish
</success_criteria>

<output>
Create `.planning/phases/04-multi-tenancy/04-03-SUMMARY.md` when done
</output>
