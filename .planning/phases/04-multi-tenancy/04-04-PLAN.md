---
phase: 04-multi-tenancy
plan: 04
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - src/index.ts
  - src/web/server.ts
  - tests/routes.test.ts
autonomous: true
requirements:
  - TENANT-02
  - TENANT-03
  - TENANT-04
  - TENANT-05
  - TENANT-06

must_haves:
  truths:
    - "Migrations rodam automaticamente no boot antes de qualquer wiring de dependencies"
    - "PgRotationStore substitui InMemoryRotationStore na instanciação de SuggestDeals"
    - "CompositeAffiliateLinkBuilder (Amazon+ML+Shopee) é passado como 4° parâmetro de SuggestDeals"
    - "GET /api/tenants retorna 200 com array de tenants"
    - "GET /api/config retorna 200 com tenant 'default'"
    - "PATCH /api/config retorna 200 com tenant atualizado"
    - "Rotas /api/tenants e /api/config são protegidas pelo middleware de auth existente"
  artifacts:
    - path: "src/index.ts"
      provides: "Boot com migration runner + PgRotationStore + CompositeAffiliateLinkBuilder"
      contains: "runMigrations"
    - path: "src/web/server.ts"
      provides: "Rotas GET /api/tenants, GET /api/config, PATCH /api/config"
      contains: "api/tenants"
  key_links:
    - from: "src/index.ts"
      to: "node-pg-migrate programmatic API"
      via: "import { migrate } from 'node-pg-migrate'; await runMigrations() antes de wiring"
      pattern: "runMigrations"
    - from: "src/index.ts"
      to: "src/web/server.ts"
      via: "setTenantRepo(tenantRepo) para injetar repo nas rotas"
      pattern: "setTenantRepo"
    - from: "src/web/server.ts"
      to: "PgTenantRepository"
      via: "_tenantRepo module-level variable + setTenantRepo setter"
      pattern: "_tenantRepo"
---

<objective>
Wirear tudo no composition root (src/index.ts) e adicionar as rotas REST faltantes em server.ts.

Wave 3: depende de todos os planos anteriores. Ativa PgRotationStore, CompositeAffiliateLinkBuilder e o boot migration runner. Adiciona GET /api/tenants, GET /api/config, PATCH /api/config.

Purpose: Este é o plano de ativação — a produção passa de InMemoryRotationStore + sem afiliados + schema defensivo para schema versionado + rotação persistida + links de afiliado + API REST completa.

Output: src/index.ts com boot runner e wiring atualizado; src/web/server.ts com 3 novas rotas; testes de rotas expandidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-tenancy/04-RESEARCH.md
@.planning/phases/04-multi-tenancy/04-01-SUMMARY.md
@.planning/phases/04-multi-tenancy/04-02-SUMMARY.md
@.planning/phases/04-multi-tenancy/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Contratos relevantes para este plano de wiring. -->

src/index.ts — imports e instanciações atuais (linhas extraídas):
  import { InMemoryRotationStore } from './adapters/store/InMemoryRotationStore.js'
  import { PgDealRepository } from './adapters/db/PgDealRepository.js'
  import { PgTenantRepository } from './adapters/db/PgTenantRepository.js'
  import { SuggestDeals } from './core/usecases/SuggestDeals.js'
  const rotationStore = new InMemoryRotationStore()
  const dealRepo = new PgDealRepository()
  const tenantRepo = new PgTenantRepository()
  const suggest = new SuggestDeals(rotationStore, dealRepo, telegramPublisher)

src/web/server.ts — auth middleware (linha 152):
  app.use('/api', (req, res, next) => { ... // DASHBOARD_PASSWORD check })
  Novas rotas DEVEM aparecer APÓS esta linha.

node-pg-migrate programmatic API (RESEARCH.md Pattern 1):
  import { migrate } from 'node-pg-migrate'
  await migrate({ databaseUrl: process.env.DATABASE_URL!, dir: path.resolve(process.cwd(), 'migrations'),
                  direction: 'up', migrationsTable: 'pgmigrations', log: (msg) => console.log('[migrations]', msg) })

AffiliateLinkBuilder wrappers criados no plano 04-02:
  AmazonAffiliateLinkBuilder — from './adapters/affiliates/AmazonAffiliate.js'
  MLAffiliateLinkBuilder — from './adapters/affiliates/MLAffiliate.js'
  ShopeeAffiliateLinkBuilder — from './adapters/affiliates/ShopeeAffiliate.js'
  CompositeAffiliateLinkBuilder — from './adapters/affiliates/CompositeAffiliateLinkBuilder.js'

PgRotationStore criado no plano 04-03:
  import { PgRotationStore } from './adapters/store/PgRotationStore.js'

PgTenantRepository — métodos relevantes:
  findAll(): Promise<Tenant[]>
  findById(id: string): Promise<Tenant | null>
  save(tenant: Tenant): Promise<void>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar rotas REST em server.ts com injeção de TenantRepository</name>
  <files>
    src/web/server.ts,
    tests/routes.test.ts
  </files>

  <read_first>
    - src/web/server.ts — LEITURA OBRIGATÓRIA COMPLETA: localizar linha 152 (auth middleware), localizar onde registrar as novas rotas APÓS o middleware; ver estrutura de imports no topo; ver exports existentes; verificar se há import de TenantRepository
    - tests/routes.test.ts — ver estrutura dos tests existentes para seguir o mesmo padrão de mock/setup e adicionar cases no mesmo style
    - src/adapters/db/PgTenantRepository.ts — ver assinatura exata de findAll, findById, save
    - src/core/domain/Tenant.ts — estrutura do tipo Tenant para validar PATCH body
  </read_first>

  <action>
    Em src/web/server.ts, realizar as seguintes alterações:

    1. Adicionar ao topo (seção de imports): import type { TenantRepository } from '../core/ports/TenantRepository.js'

    2. Adicionar variável module-level e setter (APÓS os imports, ANTES das rotas):
       let _tenantRepo: TenantRepository | null = null
       export function setTenantRepo(repo: TenantRepository): void { _tenantRepo = repo }

    3. Adicionar as três rotas APÓS app.use('/api', authMiddleware) e APÓS as rotas existentes de /api (para não interferir com /api/test-ml e /api/history que não requerem auth):
       - GET /api/tenants: const tenants = await _tenantRepo!.findAll(); res.json(tenants)
         Se _tenantRepo === null: retornar res.status(503).json({ error: 'Not initialized' })
       - GET /api/config: const tenant = await _tenantRepo!.findById('default'); se null: res.status(404).json({ error: 'Tenant not found' }); senão: res.json(tenant)
       - PATCH /api/config: const current = await _tenantRepo!.findById('default'); se null: res.status(404); const updated: Tenant = { ...current, ...(req.body as Partial<Tenant>), id: 'default', active: current.active }; await _tenantRepo!.save(updated); res.json(updated)
         SEGURANÇA OBRIGATÓRIA (T-04-09): Antes do spread, extrair apenas os campos permitidos do req.body: { filters, affiliates, channels }. Não permitir sobrescrever id ou active via PATCH body.

    4. Adicionar em tests/routes.test.ts (sem alterar testes existentes):
       - Importar setTenantRepo de server.ts
       - Criar fake TenantRepository com findAll/findById/save implementados com vi.fn()
       - Chamar setTenantRepo(fakeTenantRepo) no beforeEach
       - Test: GET /api/tenants retorna 200 com array (usa DASHBOARD_PASSWORD cookie se necessário pelo mock do middleware)
       - Test: GET /api/config retorna 200 com tenant 'default'
       - Test: GET /api/config retorna 404 quando findById retorna null
       - Test: PATCH /api/config retorna 200 com merged tenant; verifica que id não pode ser sobrescrito

    Pitfall 4 da RESEARCH: verificar que as novas rotas estão registradas DEPOIS de app.use('/api', authMiddleware). Se /api/tenants aparecer antes do middleware, ficará pública.
  </action>

  <verify>
    <automated>npx vitest run tests/routes.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "setTenantRepo" src/web/server.ts retorna >= 2 (declaração + função)
    - grep -c "api/tenants" src/web/server.ts retorna >= 1
    - grep -c "api/config" src/web/server.ts retorna >= 2 (GET e PATCH)
    - PATCH /api/config não faz spread direto de req.body sem filtrar campos — grep -c "req.body" src/web/server.ts no contexto do PATCH mostra que apenas filters/affiliates/channels são extraídos
    - vitest run tests/routes.test.ts passa com 0 falhas (existentes e novos)
    - tsc --noEmit limpo
  </acceptance_criteria>

  <done>Rotas GET /api/tenants, GET /api/config, PATCH /api/config adicionadas e testadas; setTenantRepo exportado</done>
</task>

<task type="auto">
  <name>Task 2: Wirear migration runner, PgRotationStore e CompositeAffiliateLinkBuilder em src/index.ts</name>
  <files>src/index.ts</files>

  <read_first>
    - src/index.ts — LEITURA OBRIGATÓRIA COMPLETA: ver todos os imports, a ordem de instanciação, onde InMemoryRotationStore é usado, onde SuggestDeals é instanciado (linha 36), onde server.ts é importado
    - src/adapters/store/PgRotationStore.ts — confirmar nome exato da classe e path de import
    - src/adapters/affiliates/CompositeAffiliateLinkBuilder.ts — confirmar nome exato da classe e path de import
    - src/adapters/affiliates/AmazonAffiliate.ts — confirmar nome de AmazonAffiliateLinkBuilder
    - src/adapters/affiliates/MLAffiliate.ts — confirmar nome de MLAffiliateLinkBuilder
    - src/adapters/affiliates/ShopeeAffiliate.ts — confirmar nome de ShopeeAffiliateLinkBuilder
  </read_first>

  <action>
    Em src/index.ts, realizar as seguintes alterações em ordem:

    1. Adicionar imports no topo (substituir ou acrescentar — não duplicar):
       - import path from 'path'
       - import { migrate } from 'node-pg-migrate'
       - import { PgRotationStore } from './adapters/store/PgRotationStore.js'
       - import { AmazonAffiliateLinkBuilder } from './adapters/affiliates/AmazonAffiliate.js'
       - import { MLAffiliateLinkBuilder } from './adapters/affiliates/MLAffiliate.js'
       - import { ShopeeAffiliateLinkBuilder } from './adapters/affiliates/ShopeeAffiliate.js'
       - import { CompositeAffiliateLinkBuilder } from './adapters/affiliates/CompositeAffiliateLinkBuilder.js'
       - import { setTenantRepo } from './web/server.js' (adicionar setTenantRepo ao import existente de server.ts, ou novo import se necessário)

    2. Criar função async runMigrations() ANTES do código principal:
       async function runMigrations(): Promise<void> {
         await migrate({
           databaseUrl: process.env.DATABASE_URL!,
           dir: path.resolve(process.cwd(), 'migrations'),
           direction: 'up',
           migrationsTable: 'pgmigrations',
           log: (msg: string) => console.log('[migrations]', msg),
         })
       }

    3. No body principal (que deve ser async ou já é), adicionar ANTES de qualquer instanciação de adapters/usecases:
       console.log('[boot] running migrations...')
       await runMigrations()
       console.log('[boot] migrations complete')

    4. Substituir instanciação de InMemoryRotationStore:
       ANTES: const rotationStore = new InMemoryRotationStore()
       DEPOIS: const rotationStore = new PgRotationStore()

    5. Criar composite affiliate builder APÓS instanciação de rotationStore e ANTES de SuggestDeals:
       const affiliateBuilder = new CompositeAffiliateLinkBuilder([
         new AmazonAffiliateLinkBuilder(),
         new MLAffiliateLinkBuilder(),
         new ShopeeAffiliateLinkBuilder(),
       ])

    6. Atualizar instanciação de SuggestDeals (linha ~36):
       ANTES: const suggest = new SuggestDeals(rotationStore, dealRepo, telegramPublisher)
       DEPOIS: const suggest = new SuggestDeals(rotationStore, dealRepo, telegramPublisher, affiliateBuilder)

    7. Adicionar APÓS instanciação de tenantRepo:
       setTenantRepo(tenantRepo)

    8. Remover import de InMemoryRotationStore se não usado em mais nenhum lugar de index.ts.

    Pitfall 2 da RESEARCH: usar path.resolve(process.cwd(), 'migrations') — não usar string relativa 'migrations' diretamente no migrate() call.

    VERIFICAR: node-pg-migrate v8 pode requerer que o módulo seja importado como CommonJS ou ESM dependendo do package.json "type". Se o projeto usa "type": "module", verificar se node-pg-migrate exporta ESM ou se requer createRequire(). Consultar node_modules/node-pg-migrate/dist/ para ver se há exports ESM. Se necessário, usar import { createRequire } from 'module' + require('node-pg-migrate').
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "runMigrations" src/index.ts retorna >= 2 (definição + chamada)
    - grep -c "InMemoryRotationStore" src/index.ts retorna 0 (removido)
    - grep -c "PgRotationStore" src/index.ts retorna >= 1 (instanciação)
    - grep -c "CompositeAffiliateLinkBuilder" src/index.ts retorna >= 1 (instanciação)
    - grep -c "affiliateBuilder" src/index.ts retorna >= 2 (criação e passagem para SuggestDeals)
    - grep -c "setTenantRepo" src/index.ts retorna >= 1
    - tsc --noEmit limpo
    - Ordem no código: await runMigrations() ANTES de new PgRotationStore() e new PgDealRepository() etc.
  </acceptance_criteria>

  <done>src/index.ts wireia boot migration runner, PgRotationStore, CompositeAffiliateLinkBuilder e setTenantRepo; InMemoryRotationStore removido do composition root</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar boot completo e endpoints REST em Railway (ou local com DATABASE_URL)</name>
  <what-built>
    Plano 04-04 completou o wiring de:
    - Boot migration runner (node-pg-migrate cria tabelas)
    - PgRotationStore em produção
    - CompositeAffiliateLinkBuilder injetado em SuggestDeals
    - Rotas GET /api/tenants, GET /api/config, PATCH /api/config

    Para verificar o sucesso completo da fase 4 é necessário um banco PostgreSQL real (ou staging Railway).
  </what-built>
  <how-to-verify>
    Opção A — Verificar em Railway (deploy de staging):
    1. Fazer deploy no Railway e acompanhar os logs de boot — deve aparecer "[migrations]" lines e "[boot] migrations complete"
    2. Chamar: curl -b "auth=<DASHBOARD_PASSWORD>" https://<railway-url>/api/tenants — deve retornar JSON array
    3. Chamar: curl -b "auth=<DASHBOARD_PASSWORD>" https://<railway-url>/api/config — deve retornar tenant 'default'
    4. Verificar que /api/tenants sem cookie retorna 401 ou redirect para /login

    Opção B — Verificar local (requer DATABASE_URL apontando para PostgreSQL local ou Railway):
    1. DATABASE_URL=<url> npx tsx src/index.ts — aguardar "[boot] migrations complete" nos logs
    2. curl http://localhost:<PORT>/api/tenants com header Authorization ou cookie conforme configurado

    Sucesso esperado:
    - Logs de boot mostram linhas "[migrations] ..." sem erros
    - GET /api/tenants retorna 200 com JSON array (pode conter tenant 'default' se migration 002 rodou)
    - GET /api/config retorna 200 com objeto tenant 'default'
    - GET /api/tenants sem auth retorna 401/302 (não 200)
  </how-to-verify>
  <resume-signal>Type "approved" se todos os endpoints respondem corretamente, ou descreva os erros encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| req.body → PATCH /api/config → TenantRepository.save() | User input via HTTP atravessa para o banco |
| process.env.DATABASE_URL → node-pg-migrate | String de conexão carregada no boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | PATCH /api/config — mass assignment via req.body | mitigate | Extrair apenas { filters, affiliates, channels } do req.body; forçar id: 'default' e preservar active do objeto current |
| T-04-10 | Elevation of Privilege | Novas rotas /api/tenants e /api/config sem auth middleware | mitigate | Registrar rotas APÓS app.use('/api', authMiddleware) — Pitfall 4 da RESEARCH; teste de routes.test.ts sem cookie deve retornar 401 |
| T-04-11 | Denial of Service | Migration runner falha no boot — processo termina | accept | Falha de migration deve ser fatal (não silenciada) para evitar inconsistência de schema; Railway auto-restart cuida de retry |
| T-04-12 | Tampering | DATABASE_URL exposta em logs | accept | node-pg-migrate log callback logga apenas mensagens de migração, não a URL completa |
</threat_model>

<verification>
- tsc --noEmit limpo
- vitest run tests/routes.test.ts passa
- grep -c "InMemoryRotationStore" src/index.ts = 0
- grep -c "runMigrations" src/index.ts >= 2
- grep -c "setTenantRepo" src/index.ts >= 1
- grep -c "api/tenants" src/web/server.ts >= 1
- Checkpoint humano confirmou boot + endpoints em ambiente com banco real
</verification>

<success_criteria>
1. src/index.ts chama runMigrations() antes de qualquer wiring
2. PgRotationStore substituiu InMemoryRotationStore em index.ts
3. CompositeAffiliateLinkBuilder(Amazon+ML+Shopee) passado como 4° argumento de SuggestDeals
4. GET /api/tenants, GET /api/config, PATCH /api/config respondem corretamente
5. PATCH /api/config não permite sobrescrever id via body
6. Todas as novas rotas protegidas pelo middleware de auth existente
</success_criteria>

<output>
Create `.planning/phases/04-multi-tenancy/04-04-SUMMARY.md` when done
</output>
