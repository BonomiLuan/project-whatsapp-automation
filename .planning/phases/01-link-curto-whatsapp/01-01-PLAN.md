---
phase: 01-link-curto-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/links.test.ts
  - tests/routes.test.ts
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-07

must_haves:
  truths:
    - "vitest run exits 0 on stub test files"
    - "Test files import from src/server/links.ts and src/server/index.ts using correct ESM .js paths"
    - "Test structure covers: generateCode uniqueness, SSRF allowlist, /r/:code HTML, /img/:code proxy, /api/links auth"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner config for ESM + TypeScript project"
      contains: "defineConfig"
    - path: "tests/links.test.ts"
      provides: "Unit test stubs for links.ts module"
      contains: "generateUniqueCode"
    - path: "tests/routes.test.ts"
      provides: "Integration test stubs for HTTP routes"
      contains: "/r/:code"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "resolve.conditions NodeNext"
      pattern: "resolve"
---

<objective>
Install vitest and create test scaffold (config + stub test files) for all behaviors that automated verification will check after Plans 02–04.

Purpose: The Nyquist Rule requires every task to have automated verification. Plans 02–04 implement code; their verify commands must exist before implementation. This plan creates the test infrastructure so executors in later waves have runnable test targets.

Output: vitest.config.ts, tests/links.test.ts (unit stubs), tests/routes.test.ts (integration stubs). All files are stubs — they define describe/it blocks with correct imports but mark tests as todo() or skip() pending implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-link-curto-whatsapp/01-CONTEXT.md
@.planning/phases/01-link-curto-whatsapp/01-RESEARCH.md
@tsconfig.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create vitest.config.ts</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - package.json — confirm vitest is not already present; see existing devDependencies and scripts
    - tsconfig.json — confirm moduleResolution: NodeNext so config can match
  </read_first>
  <action>
    Run: npm install --save-dev vitest

    Create vitest.config.ts at project root. The project uses ESM (type: "module" in package.json) and NodeNext module resolution. Config must:
    - Import defineConfig from "vitest/config"
    - Set test.environment to "node"
    - Set resolve.conditions to ["node"] to match NodeNext resolution
    - Set test.include to ["tests/**/*.test.ts"]
    - Set test.globals to false (use explicit imports)

    Add a "test" script to package.json scripts: "vitest run --reporter=verbose"
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx vitest run --reporter=verbose 2>&1 | grep -E "Test Files|No test files"</automated>
  </verify>
  <done>vitest.config.ts exists; "npm test" runs without config errors; "Test Files" or "No test files found" appears in output (no crash)</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for links module and HTTP routes</name>
  <files>tests/links.test.ts, tests/routes.test.ts</files>
  <read_first>
    - .planning/phases/01-link-curto-whatsapp/01-RESEARCH.md — Validation Architecture section, Phase Requirements → Test Map table
    - .planning/phases/01-link-curto-whatsapp/01-CONTEXT.md — decisions D-04 (SSRF allowlist domains), D-08 (LRU cache)
  </read_first>
  <action>
    Create tests/links.test.ts with:
    - import { describe, it, expect } from "vitest"
    - describe block "generateUniqueCode" — it.todo("generates 5-char alphanumeric codes")
    - describe block "generateUniqueCode" — it.todo("1000 calls produce no duplicates")
    - describe block "isSsrfAllowed" — it.todo("allows *.shopee.com.br")
    - describe block "isSsrfAllowed" — it.todo("allows *.mlstatic.com")
    - describe block "isSsrfAllowed" — it.todo("blocks arbitrary domain")
    - describe block "buildExpiredRedirectUrl" — it.todo("shopee source builds search URL")
    - describe block "buildExpiredRedirectUrl" — it.todo("amazon source includes AMAZON_TAG")
    - describe block "buildExpiredRedirectUrl" — it.todo("ml source includes ML_PUBLISHER_ID")

    NOTE: Do NOT import from src/server/links.ts yet — the module does not exist. Use it.todo() for all tests. Real imports will be added in Plan 02 after links.ts is created.

    Create tests/routes.test.ts with:
    - import { describe, it, expect } from "vitest"
    - describe block "GET /r/:code" — it.todo("valid code returns 200 HTML with og:title")
    - describe block "GET /r/:code" — it.todo("valid code HTML contains og:image with /img/ path")
    - describe block "GET /r/:code" — it.todo("valid code HTML contains meta http-equiv refresh")
    - describe block "GET /r/:code" — it.todo("unknown code returns 404")
    - describe block "GET /r/:code" — it.todo("expired link redirects to platform search URL (302)")
    - describe block "GET /img/:code" — it.todo("valid code returns 200 with image content-type")
    - describe block "GET /img/:code" — it.todo("unknown code returns 404")
    - describe block "GET /api/links" — it.todo("without auth returns 401")
    - describe block "GET /api/links" — it.todo("with auth cookie returns array")

    NOTE: Same — do NOT import from src/server/index.ts yet. All tests are it.todo().
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx vitest run --reporter=verbose 2>&1 | grep -E "todo|Tests|PASS|FAIL"</automated>
  </verify>
  <done>Both test files exist; `npx vitest run` shows 0 failed, all tests listed as "todo"; no TypeScript compile errors in the test files themselves</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry | npm install fetches vitest package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install vitest | mitigate | Package legitimacy audit in RESEARCH.md confirms all packages [OK]; vitest is a first-party Vite ecosystem package with 10+ years of ecosystem trust |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH.md Package Legitimacy Audit table confirms vitest [OK] — no postinstall scripts, official Vite ecosystem |
</threat_model>

<verification>
- tests/ directory exists with links.test.ts and routes.test.ts
- vitest.config.ts exists at project root
- `npx vitest run` exits 0 (all tests are todo, not failing)
- package.json has "test" script invoking vitest
</verification>

<success_criteria>
`npx vitest run` completes without errors; test output shows todo stubs for all 17 behaviors listed in the test files; no TypeScript errors in test files.
</success_criteria>

<output>
Create `.planning/phases/01-link-curto-whatsapp/01-01-SUMMARY.md` when done
</output>
