---
phase: 01-link-curto-whatsapp
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - src/telegram/bot.ts
  - src/api/mercadoLivreAffiliate.ts
  - .env.example
autonomous: false
requirements:
  - REQ-05
  - REQ-06

user_setup:
  - service: railway-postgres
    why: "DATABASE_URL must point to a provisioned Railway Postgres service"
    env_vars:
      - name: DATABASE_URL
        source: "Railway Dashboard → your project → New Service → Database → PostgreSQL → Connect → copy DATABASE_URL. Then set as Reference Variable on your Express service: Settings → Variables → Add → DATABASE_URL = ${{Postgres.DATABASE_URL}}"
      - name: BASE_URL
        source: "Set to https://ofertas.thaisbonomi.com.br in Railway Express service environment variables"
    dashboard_config:
      - task: "Provision Postgres service"
        location: "Railway Dashboard → New Service → Database → PostgreSQL"
  - service: amazon-affiliate
    why: "AMAZON_TAG used in expired-link redirect URLs"
    env_vars:
      - name: AMAZON_TAG
        source: "Amazon Associates → your tag ID (e.g. seutag-20) — already in .env.example as AMAZON_ASSOCIATE_TAG; both names accepted"

must_haves:
  truths:
    - "sendToTelegram caption contains BASE_URL/r/{code} instead of the long affiliate URL"
    - "sendDealToChat caption contains BASE_URL/r/{code} instead of the long dealUrl"
    - "sendDealCard formatMessage receives shortUrl; inline button keeps original dealUrl"
    - "sendProductToChat caption contains BASE_URL/r/{code}"
    - "createLink failure in any bot function falls back to original URL without crashing the send"
    - "buildMLSearchUrl exported from mercadoLivreAffiliate.ts"
    - ".env.example contains DATABASE_URL and BASE_URL entries"
  artifacts:
    - path: "src/telegram/bot.ts"
      provides: "Short link integration in all 3 send functions + detectSource helper"
      contains: "createLink"
    - path: "src/api/mercadoLivreAffiliate.ts"
      provides: "Exported buildMLSearchUrl function"
      contains: "export function buildMLSearchUrl"
    - path: ".env.example"
      provides: "Documentation for new env vars"
      contains: "DATABASE_URL"
  key_links:
    - from: "sendToTelegram / sendDealToChat / sendDealCard / sendProductToChat"
      to: "createLink()"
      via: "import from ../server/links.js"
      pattern: "createLink"
    - from: "buildExpiredRedirectUrl"
      to: "buildMLSearchUrl"
      via: "uses same PUBLISHER_ID / MATT_WORD env vars"
      pattern: "ML_PUBLISHER_ID"
---

<objective>
Patch the four Telegram bot send functions to generate a short link before composing captions, and export buildMLSearchUrl from mercadoLivreAffiliate.ts for use in expired-redirect fallback.

Purpose: This is the user-visible deliverable — captions sent to Telegram will contain the short link that generates WhatsApp preview when copied. Without this plan, the storage and HTTP infrastructure exists but is never used.

Output: src/telegram/bot.ts patched at sendToTelegram, sendDealToChat, sendDealCard, sendProductToChat. src/api/mercadoLivreAffiliate.ts exports buildMLSearchUrl. .env.example documents DATABASE_URL, BASE_URL, AMAZON_TAG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-link-curto-whatsapp/01-CONTEXT.md
@.planning/phases/01-link-curto-whatsapp/01-RESEARCH.md
@.planning/phases/01-link-curto-whatsapp/01-PATTERNS.md
@src/telegram/bot.ts
@src/api/mercadoLivreAffiliate.ts
@.env.example
@.planning/phases/01-link-curto-whatsapp/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan imports -->

From src/server/links.ts (already built in Plan 02):
```typescript
export function createLink(data: {
  title: string; image_url: string; affiliate_url: string;
  source: 'shopee' | 'amazon' | 'ml'
}): Promise<LinkEntry>
```

From src/telegram/bot.ts — current function signatures and relevant line numbers:
- sendToTelegram(ctx: Ctx, product: ProductData, coupon: string): async function at line 75
  - Uses buildTelegramText(product, coupon, groupUrl) where product.originalUrl is the affiliate URL
- sendDealToChat(deal: UnifiedDeal): exported async at line 721
  - let dealUrl = deal.affiliateUrl (line 728); formatMessage uses buyUrl: dealUrl (line 745)
- sendDealCard(ctx: Ctx, deal: UnifiedDeal): async at line 795
  - let dealUrl = deal.affiliateUrl (line 798); formatMessage uses buyUrl: dealUrl (line 820)
  - Markup.button.url keeps dealUrl (line 825)
- sendProductToChat(product: ProductData, coupon: string): exported async at line 769
  - Uses buildTelegramText(product, coupon, groupUrl) (line 777)

From src/api/mercadoLivreAffiliate.ts:
- const PUBLISHER_ID = process.env.ML_PUBLISHER_ID ?? '64897511' (line 3, module-level)
- const MATT_WORD = process.env.ML_MATT_WORD ?? 'mamaeeconomica' (line 4, module-level)
- These are NOT exported — buildMLSearchUrl can reference them directly since it lives in the same module
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Export buildMLSearchUrl from mercadoLivreAffiliate.ts and update .env.example</name>
  <files>src/api/mercadoLivreAffiliate.ts, .env.example</files>
  <read_first>
    - src/api/mercadoLivreAffiliate.ts — lines 1-10: verify PUBLISHER_ID and MATT_WORD are module-level constants; lines 100-117: injectMLTag pattern to mirror
    - .env.example — current content; see where to append new vars without breaking existing sections
    - .planning/phases/01-link-curto-whatsapp/01-PATTERNS.md — mercadoLivreAffiliate.ts section
  </read_first>
  <action>
    In src/api/mercadoLivreAffiliate.ts, add after the existing exports (append at end of file):

    export function buildMLSearchUrl(title: string): string {
      return 'https://www.mercadolivre.com.br/jm/search?as_word=' + encodeURIComponent(title) + '&matt_tool=' + PUBLISHER_ID + '&matt_word=' + MATT_WORD
    }

    This function references the already-declared module-level PUBLISHER_ID and MATT_WORD constants directly. No new imports needed.

    In .env.example, add a new section after the existing "# App" block:

    # Link Shortener (Phase 1)
    DATABASE_URL=postgresql://user:password@host:5432/railway
    BASE_URL=https://ofertas.thaisbonomi.com.br
    AMAZON_TAG=seutag-20
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "export function buildMLSearchUrl" src/api/mercadoLivreAffiliate.ts returns a match
    - grep "DATABASE_URL" .env.example returns a match
    - grep "BASE_URL" .env.example returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>buildMLSearchUrl is exported from mercadoLivreAffiliate.ts; .env.example documents all 3 new env vars; no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Patch bot.ts — add createLink integration to all 4 send functions</name>
  <files>src/telegram/bot.ts</files>
  <read_first>
    - src/telegram/bot.ts — read lines 1-11 (imports), lines 58-95 (buildTelegramText + sendToTelegram), lines 721-790 (sendDealToChat + sendProductToChat), lines 795-838 (sendDealCard)
    - .planning/phases/01-link-curto-whatsapp/01-PATTERNS.md — bot.ts patch section (all 4 integration points + detectSource helper)
    - .planning/phases/01-link-curto-whatsapp/01-RESEARCH.md — Code Examples (bot integration pattern), Pitfall 6 (async createLink in sendDealToChats loop), Common Pitfalls section
  </read_first>
  <action>
    ADD import to bot.ts import section (after existing imports, use .js extension):
    import { createLink } from '../server/links.js'

    ADD detectSource helper function immediately before sendToTelegram (line 75):
    function detectSource(url: string): 'shopee' | 'amazon' | 'ml' {
      if (url.includes('shopee')) return 'shopee'
      if (url.includes('amazon') || url.includes('amzn')) return 'amazon'
      return 'ml'
    }

    PATCH sendToTelegram (line 75-88):
    After 'const groupUrl = ...' and BEFORE 'const text = buildTelegramText(...)':
    - Declare: let shortUrl = product.originalUrl
    - try { const link = await createLink({ title: product.name, image_url: product.imageUrl ?? '', affiliate_url: product.originalUrl, source: detectSource(product.originalUrl) }); shortUrl = (process.env.BASE_URL || '') + '/r/' + link.code } catch (err) { console.warn('[links] createLink failed in sendToTelegram, using original URL:', err) }
    - Change: const text = buildTelegramText({ ...product, originalUrl: shortUrl }, coupon, groupUrl)

    PATCH sendDealToChat (line 721, exported): After the existing 'dealUrl' re-assignment block (the generateAffiliateLink try/catch block ending around line 738) and BEFORE the formatMessage call (line 740):
    - Declare: let shortUrl = dealUrl
    - try { const link = await createLink({ title: deal.title, image_url: deal.imageUrl ?? '', affiliate_url: dealUrl, source: deal.source === 'mercado-livre' ? 'ml' : deal.source === 'shopee' ? 'shopee' : 'amazon' }); shortUrl = (process.env.BASE_URL || '') + '/r/' + link.code } catch (err) { console.warn('[links] createLink failed in sendDealToChat:', err) }
    - Change formatMessage call: buyUrl: shortUrl (was: buyUrl: dealUrl)
    - Keep Markup.button.url using dealUrl (the inline button is Telegram-only; keep long URL for direct-click)

    PATCH sendProductToChat (line 769): After 'const groupUrl = ...' (line 776) and BEFORE 'const text = buildTelegramText(...)' (line 777):
    - Declare: let shortUrl = product.originalUrl
    - try { const link = await createLink({ title: product.name, image_url: product.imageUrl ?? '', affiliate_url: product.originalUrl, source: detectSource(product.originalUrl) }); shortUrl = (process.env.BASE_URL || '') + '/r/' + link.code } catch (err) { console.warn('[links] createLink failed in sendProductToChat:', err) }
    - Change: const text = buildTelegramText({ ...product, originalUrl: shortUrl }, coupon, groupUrl)

    PATCH sendDealCard (line 795): After the existing generateAffiliateLink try/catch block (ends around line 808) and BEFORE the formatMessage call (line 815):
    - Declare: let shortUrl = dealUrl
    - try { const link = await createLink({ title: deal.title, image_url: deal.imageUrl ?? '', affiliate_url: dealUrl, source: deal.source === 'mercado-livre' ? 'ml' : deal.source === 'shopee' ? 'shopee' : 'amazon' }); shortUrl = (process.env.BASE_URL || '') + '/r/' + link.code } catch (err) { console.warn('[links] createLink failed in sendDealCard:', err) }
    - Change formatMessage call: buyUrl: shortUrl (was: buyUrl: dealUrl)
    - Keep Markup.button.url in buttons using dealUrl (line 825) — Telegram-only button, keep long URL
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - grep -c "createLink" src/telegram/bot.ts returns 5 (1 import + 4 call sites)
    - grep -c "console.warn.*createLink failed" src/telegram/bot.ts returns 4
    - grep "detectSource" src/telegram/bot.ts exists
    - grep "shortUrl.*BASE_URL.*\/r\/" src/telegram/bot.ts has matches in all 4 send functions
    - grep "Markup.button.url.*dealUrl" src/telegram/bot.ts still exists (button keeps long URL)
  </acceptance_criteria>
  <done>bot.ts imports createLink; all 4 send functions generate short link before caption; each has try/catch fallback; tsc --noEmit exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-04 complete the full link shortener:
    - src/server/links.ts — Postgres storage module with pool, DDL, CRUD, SSRF allowlist
    - src/server/index.ts — GET /r/:code (OG HTML, rate-limited), GET /img/:code (image proxy, LRU cache), GET /api/links (auth-protected)
    - src/telegram/bot.ts — 4 send functions now generate short links before composing captions
    - src/api/mercadoLivreAffiliate.ts — exports buildMLSearchUrl
    - .env.example — documents DATABASE_URL, BASE_URL, AMAZON_TAG

    Pre-requisite before testing: Provision Postgres on Railway (Dashboard → New Service → Database → PostgreSQL), set DATABASE_URL reference variable on Express service, set BASE_URL=https://ofertas.thaisbonomi.com.br, add DATABASE_URL to local .env.
  </what-built>
  <how-to-verify>
    1. Provision Railway Postgres and set env vars as described in user_setup frontmatter above.

    2. Start server locally:
       npm run dev
       Confirm in logs: no "DATABASE_URL is not defined" error; server starts on port 3000.

    3. Test storage layer (needs DB):
       curl -X POST http://localhost:3000/api/scrape -H "Content-Type: application/json" -d '{"url":"https://shopee.com.br"}' 
       (This just confirms server is up; actual link creation happens via bot)

    4. Test /r/:code — create a test link in the DB directly or trigger the bot wizard:
       - Send /start to the Telegram bot, go through the wizard, confirm a product
       - In the caption of the photo received in Telegram, confirm it contains "ofertas.thaisbonomi.com.br/r/" and does NOT contain the original Shopee/Amazon/ML URL

    5. Test WhatsApp preview:
       - Copy the caption text from Telegram
       - Paste the short URL into WhatsApp (any chat)
       - Confirm WhatsApp shows a preview card with the product image and title (may take a few seconds to load)
       - Confirm the preview image is the product photo (not a broken image)

    6. Test redirect:
       curl -L http://localhost:3000/r/{CODE_FROM_STEP_4}
       - Should follow through to the affiliate URL (Shopee/Amazon/ML product page)

    7. Test analytics:
       curl -b "auth=$(node -e "const c=require('crypto');console.log(c.createHmac('sha256',process.env.DASHBOARD_PASSWORD||'admin').update('auth').digest('hex'))")" http://localhost:3000/api/links
       - Should return JSON array with the link created in step 4
       - click_count should be 1 or more if you visited /r/:code

    8. Test auto-deal (optional — if deals are cached):
       - Wait for a 15-min cron tick or call /api/suggest manually
       - Check Telegram for the auto-deal message — caption should contain /r/ link
  </how-to-verify>
  <resume-signal>Type "approved" if WhatsApp preview shows product image and title when you paste the short URL. Describe any issues if not working.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot.ts → createLink() | product data from scraper/affiliate APIs crosses into DB insert |
| createLink try/catch | DB failure must not propagate to break message send loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Denial of Service | sendDealToChat createLink failure | mitigate | try/catch with console.warn fallback — per RESEARCH.md Pitfall 6. DB failure degrades gracefully: deal still sends with original URL |
| T-01-13 | Spoofing | affiliate_url passed to createLink from bot | mitigate | createLink validates affiliate_url domain via ALLOWED_AFFILIATE_PREFIXES check in links.ts — per D-04 |
| T-01-14 | Info Disclosure | BASE_URL in bot.ts | accept | BASE_URL is a public domain (ofertas.thaisbonomi.com.br) — intentionally public for OG tag short links |
| T-01-SC | Tampering | npm installs | accept | No new npm installs in this plan |
</threat_model>

<verification>
- npx tsc --noEmit exits 0 after all 3 file changes
- grep -c "createLink" src/telegram/bot.ts returns 5
- grep -c "console.warn.*createLink failed" src/telegram/bot.ts returns 4
- grep "export function buildMLSearchUrl" src/api/mercadoLivreAffiliate.ts returns match
- grep "DATABASE_URL" .env.example returns match
- Human checkpoint: WhatsApp preview shows product image when short URL pasted
</verification>

<success_criteria>
- tsc --noEmit exits 0 across all modified files
- Telegram bot wizard post produces caption with /r/ short URL, not the original affiliate URL
- WhatsApp shows OG preview when the short URL is pasted into a chat
- /api/links returns analytics with click counts after visiting the short link
</success_criteria>

<output>
Create `.planning/phases/01-link-curto-whatsapp/01-04-SUMMARY.md` when done
</output>
