---
phase: 02-hexagonal-structure
plan: 06
type: execute
wave: 6
depends_on:
  - 02-05
files_modified:
  - src/adapters/publishers/TelegramPublisher.ts
  - src/web/server.ts
  - src/jobs/monitorPelando.ts
  - src/jobs/monitorML.ts
  - src/jobs/cronLock.ts
  - src/index.ts
  - package.json
autonomous: true
requirements:
  - STRUCT-05
  - STRUCT-10

must_haves:
  truths:
    - "src/adapters/publishers/TelegramPublisher.ts exists (moved from telegram/bot.ts)"
    - "src/web/server.ts exists (moved from server/index.ts)"
    - "src/jobs/monitorPelando.ts, monitorML.ts, cronLock.ts exist with cron.schedule() calls extracted from web/server.ts"
    - "src/index.ts exists as composition root stub"
    - "package.json main, dev script, start script all point to new paths"
    - "tsc --noEmit passes after each commit"
    - "npm test passes"
  artifacts:
    - path: "src/adapters/publishers/TelegramPublisher.ts"
      provides: "Telegram bot god-file (moved)"
    - path: "src/web/server.ts"
      provides: "Express server god-file (moved)"
    - path: "src/jobs/monitorPelando.ts"
      provides: "Cron for monitorPelando extracted from server/index.ts"
    - path: "src/jobs/monitorML.ts"
      provides: "Cron for refreshDeals extracted from server/index.ts"
    - path: "src/jobs/cronLock.ts"
      provides: "Cron for sendNextSuggestion + daily reset extracted from server/index.ts"
    - path: "src/index.ts"
      provides: "New composition root — starts app"
  key_links:
    - from: "src/jobs/monitorPelando.ts"
      to: "src/web/server.ts"
      via: "import { monitorPelando }"
      pattern: "from.*web/server"
    - from: "src/adapters/publishers/TelegramPublisher.ts"
      to: "src/web/server.ts"
      via: "import { type UnifiedDeal }"
      pattern: "web/server"
    - from: "package.json"
      to: "src/index.ts"
      via: "main and scripts.start point to dist/index.js"
      pattern: "dist/index.js"
---

<objective>
Move the two god-files (bot.ts → TelegramPublisher.ts, server/index.ts → web/server.ts), extract 4 cron.schedule() calls into 3 job files, and create the composition root src/index.ts. Update package.json to keep Railway deployable. This is the final wave — most complex ordering.

Purpose: Complete the hexagonal structure. All files are now in their correct locations.
Output: TelegramPublisher.ts, web/server.ts, 3 job files, src/index.ts; old src/server/ and src/telegram/ directories gone.
</objective>

<execution_context>
@/Users/bonomiluan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bonomiluan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hexagonal-structure/02-CONTEXT.md
@.planning/phases/02-hexagonal-structure/02-RESEARCH.md
</context>

<interfaces>
<!-- Commit order within Wave F is critical — must be sequential -->

Commit order (per RESEARCH.md Wave F + D-03):
  F1: Move telegram/bot.ts → src/adapters/publishers/TelegramPublisher.ts
      Update in same commit: src/server/index.ts (import of createBot, sendProductToChat, sendDealToChat)
      Fix in TelegramPublisher.ts: import of UnifiedDeal type from '../server/index.js' → '../../web/server.js'
        (path is pre-computed: TelegramPublisher is at src/adapters/publishers/, web/server.ts is at src/web/)

  F2: Extract 3 cron job files from src/server/index.ts
      src/jobs/monitorPelando.ts — imports monitorPelando from '../web/server.js'
      src/jobs/monitorML.ts — imports refreshDeals from '../web/server.js'
      src/jobs/cronLock.ts — imports sendNextSuggestion from '../web/server.js'; also has the daily reset cron
      Remove cron.schedule() calls from server/index.ts (lines 582, 658-660); keep function definitions

  F3: Move src/server/index.ts → src/web/server.ts
      Update all imports inside the file to reflect new location (relative paths change from src/server/ to src/web/)
      BEFORE this commit: src/server/index.ts already had imports updated through waves 02-03 and 02-05
      All remaining relative imports in index.ts (by Wave F): adapters paths are already absolute-ish;
        verify that '../adapters/...' references are correct from src/web/ (they should be since adapters/ is sibling of web/)
      Update package.json in same commit: main → "dist/index.js", scripts.start → "node dist/index.js", scripts.dev → "tsx watch src/index.ts"

  F4: Create src/index.ts as composition root stub
      Imports from './web/server.js' and starts the app (or just re-exports the start)

Cron extraction detail (RESEARCH.md verified — cron.schedule calls at these lines):
  Line 582: cron.schedule('0 0 * * *', inline_reset_fn) → cronLock.ts (daily reset of sentToday + roundRobinIndex)
  Line 658: cron.schedule('*/30 * * * *', refreshDeals, ...)  → monitorML.ts
  Line 659: cron.schedule('*/30 * * * *', monitorPelando, ...) → monitorPelando.ts
  Line 660: cron.schedule('*/15 7-22 * * *', sendNextSuggestion, ...) → cronLock.ts

  The inline reset function at line 582 must be extracted into cronLock.ts (it resets sentToday and roundRobinIndex variables defined in server/index.ts).
  IMPORTANT: sentToday and roundRobinIndex are in-memory variables in server/index.ts. Moving the cron to a job file means the job file must import them or the reset must be moved too. Per D-01c: "move puro das chamadas de cron, sem alterar a lógica das funções chamadas". The reset cron at line 582 is inline (no named function) — extract to a named function resetDailyState in web/server.ts, export it, and import it from cronLock.ts.

Relative path shifts (TelegramPublisher.ts at src/adapters/publishers/):
  Old bot.ts paths → TelegramPublisher.ts paths:
    '../scraper/productScraper.js' → already updated to '../adapters/scrapers/ProductScraper.js' in plan 02-03? 
    No — bot.ts itself still at src/telegram/ — the IMPORTERS updated their paths but bot.ts internal paths were NOT changed.
    When bot.ts moves to src/adapters/publishers/TelegramPublisher.ts, its OWN internal imports must be recalculated:
      '../scraper/productScraper.js' → './ProductScraper.js' (sibling in scrapers? No — publishers vs scrapers)
        Actually: from src/adapters/publishers/ to src/adapters/scrapers/ = '../scrapers/ProductScraper.js'
      '../content/messageBuilder.js' → './format.js' (same publishers dir)
      '../api/metaClient.js' → './WhatsAppPublisher.js' (same publishers dir)
      '../api/shopeeAffiliate.js' → '../affiliates/ShopeeAffiliate.js'
      '../api/amazonAffiliate.js' → '../affiliates/AmazonAffiliate.js'
      '../api/mercadoLivreAffiliate.js' → '../affiliates/MLAffiliate.js'
      '../server/links.js' → '../db/PgLinkRepository.js'
        (withCronLock from lock: '../lock/PgAdvisoryLock.js' — check if bot.ts imports withCronLock)
      '../server/index.js' (type import of UnifiedDeal) → '../../web/server.js'
    All these paths must be recalculated when copying bot.ts content to TelegramPublisher.ts.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move bot.ts → TelegramPublisher.ts (Commit F1)</name>
  <files>
    src/adapters/publishers/TelegramPublisher.ts
    src/server/index.ts
  </files>
  <read_first>
    src/telegram/bot.ts — FULL content. Read every import statement at the top. All imports must be recalculated for the new file location at src/adapters/publishers/.
    src/server/index.ts — the import of createBot, sendProductToChat, sendDealToChat from bot.ts (currently '../telegram/bot.js')
  </read_first>
  <action>
    Copy src/telegram/bot.ts to src/adapters/publishers/TelegramPublisher.ts.

    In TelegramPublisher.ts, update ALL internal import paths for the new file location (src/adapters/publishers/):
    - '../scraper/productScraper.js' → '../scrapers/ProductScraper.js'
    - '../content/messageBuilder.js' → './format.js'
    - '../api/metaClient.js' → './WhatsAppPublisher.js'
    - '../api/shopeeAffiliate.js' → '../affiliates/ShopeeAffiliate.js'
    - '../api/amazonAffiliate.js' → '../affiliates/AmazonAffiliate.js'
    - '../api/mercadoLivreAffiliate.js' → '../affiliates/MLAffiliate.js'
    - '../server/links.js' imports: split into two:
        link functions (createLink, cleanupLinks, truncateLinks, markDealVote, updateLinkImage) → from '../db/PgLinkRepository.js'
        lock functions if imported (withCronLock etc.) → from '../lock/PgAdvisoryLock.js'
    - '../server/index.js' (type UnifiedDeal import) → '../../web/server.js'
      NOTE: web/server.ts does not exist yet at commit time, but this is a type-only import; tsc still needs the path to be valid. Since web/server.ts will be created in F3, and the type UnifiedDeal is defined there, this path will compile once web/server.ts exists. For now: set the path to '../../web/server.js' and continue. tsc may error until F3 — handle by creating web/server.ts in F1 as a minimal stub that exports type UnifiedDeal, then replace it with the full content in F3.
      ALTERNATIVE: If the full move is done in this single commit, create web/server.ts with the full content of server/index.ts in F1 (instead of F3), effectively merging F1 and F3. This avoids the partial-compilation problem. The planner recommends this alternative: MERGE F1 (bot.ts move) and F3 (server/index.ts move) into one commit if the intermediate state (F1 alone) fails tsc due to the '../server/index.js' circular type import.
      Decision to executor: Check if setting the type import to '../../web/server.js' causes tsc failure when web/server.ts does not yet exist. If it does: merge F1 + F3 into one commit. If tsc passes anyway (type-only imports are erased; may not require the file to exist), do F1 then F3 separately.

    In src/server/index.ts: update the import of createBot, sendProductToChat, sendDealToChat from '../telegram/bot.js' to '../adapters/publishers/TelegramPublisher.js'.

    Delete src/telegram/bot.ts.

    Run npx tsc --noEmit. If it fails only due to missing '../../web/server.js' (UnifiedDeal), proceed to F3 in the same commit (merge strategy). Otherwise commit F1 now and proceed to Task 2.

    Commit message (if separate): "refactor(02): move telegram/bot.ts → adapters/publishers/TelegramPublisher (F1)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/publishers/TelegramPublisher.ts exists
    - src/telegram/bot.ts does NOT exist
    - grep "telegram/bot" src/server/index.ts returns ZERO matches
    - grep "adapters/publishers/TelegramPublisher" src/server/index.ts returns a match
    - All import paths inside TelegramPublisher.ts point to adapters/ subdirectories (grep "\.\.\/api\|\.\.\/content\|\.\.\/scraper\|\.\.\/server" src/adapters/publishers/TelegramPublisher.ts returns ZERO matches)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TelegramPublisher.ts at new path with all internal imports recalculated, index.ts import updated, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract cron jobs + move server/index.ts → web/server.ts + update package.json (Commits F2+F3+F4)</name>
  <files>
    src/jobs/monitorPelando.ts
    src/jobs/monitorML.ts
    src/jobs/cronLock.ts
    src/web/server.ts
    src/index.ts
    package.json
  </files>
  <read_first>
    src/server/index.ts — FULL content. Read all 4 cron.schedule() calls (lines 582, 658-660) and the functions they call (monitorPelando, refreshDeals, sendNextSuggestion). Note which variables the inline reset function at line 582 touches (sentToday, roundRobinIndex — must export a named function from web/server.ts).
    package.json — main, scripts.dev, scripts.start current values
  </read_first>
  <action>
    STEP F2 — Extract cron jobs (modify src/server/index.ts in-place before moving it):

    In src/server/index.ts: add export keyword to monitorPelando, refreshDeals, and sendNextSuggestion function declarations (so job files can import them). Also extract the inline reset callback at line 582 into a named exported function resetDailyState() that resets sentToday (clear the Set) and roundRobinIndex (set to 0).

    Remove the 4 cron.schedule() calls (lines 582, 658-660) from src/server/index.ts. The function bodies stay.

    Create src/jobs/monitorPelando.ts: import cron from 'node-cron'; import { monitorPelando } from '../web/server.js'; cron.schedule('*/30 * * * *', monitorPelando, { timezone: 'America/Sao_Paulo' })
    Create src/jobs/monitorML.ts: import cron from 'node-cron'; import { refreshDeals } from '../web/server.js'; cron.schedule('*/30 * * * *', refreshDeals, { timezone: 'America/Sao_Paulo' })
    Create src/jobs/cronLock.ts: import cron from 'node-cron'; import { sendNextSuggestion, resetDailyState } from '../web/server.js'; cron.schedule('*/15 7-22 * * *', sendNextSuggestion, { timezone: 'America/Sao_Paulo' }); cron.schedule('0 0 * * *', resetDailyState, { timezone: 'America/Sao_Paulo' })

    Use the exact cron expressions from the original lines 582, 658-660 of server/index.ts (read the file to confirm before writing).

    STEP F3 — Move server/index.ts → web/server.ts:

    Copy src/server/index.ts (now without cron.schedule() calls, with exported functions) to src/web/server.ts.

    In web/server.ts: verify all relative import paths are correct from the new location src/web/. The imports were previously relative from src/server/. Key paths to check and update:
    - '../adapters/...' from src/server/ becomes '../adapters/...' from src/web/ — SAME relative path (both are one level under src/). No change needed.
    - '../telegram/bot.js' should already be '../adapters/publishers/TelegramPublisher.js' from F1. Verify.
    - Any remaining paths pointing to src/server/ subdirs (none should remain after prior waves).

    Delete src/server/index.ts.

    STEP F4 — Create src/index.ts (composition root stub) and update package.json:

    Create src/index.ts: import './web/server.js'; import './jobs/monitorPelando.js'; import './jobs/monitorML.js'; import './jobs/cronLock.js'; (This is the composition root — it boots server and registers cron jobs.)

    Update package.json:
    - "main": "dist/index.js"
    - "scripts.dev": "tsx watch src/index.ts"
    - "scripts.start": "node dist/index.js"

    COMMIT STRATEGY: Because job files import from '../web/server.js' and web/server.ts is created in the same step, all these changes should go into one large atomic commit. Run npx tsc --noEmit after all files are in place. Fix errors. Run npm test.

    Commit: "refactor(02): move server/index.ts → web/server.ts + extract cron jobs + add composition root (F2+F3+F4)".

    After commit: remove empty directories src/server/ and src/telegram/ if they are now empty.
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/web/server.ts exists
    - src/server/index.ts does NOT exist
    - src/server/ directory does NOT exist (or is empty)
    - src/telegram/ directory does NOT exist (all files moved)
    - src/jobs/monitorPelando.ts exists and contains cron.schedule
    - src/jobs/monitorML.ts exists and contains cron.schedule
    - src/jobs/cronLock.ts exists and contains cron.schedule (two calls)
    - src/index.ts exists and imports from ./web/server.js
    - grep '"main"' package.json shows "dist/index.js"
    - grep '"start"' package.json shows "node dist/index.js"
    - grep '"dev"' package.json shows "tsx watch src/index.ts"
    - grep "cron.schedule" src/web/server.ts returns ZERO matches (all cron calls extracted)
    - npx tsc --noEmit exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>God-files moved, cron jobs extracted, composition root created, package.json updated, tsc and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete hexagonal structure created across 6 plans and approximately 15+ atomic commits. All source files are now in their hexagonal locations. package.json points to new entry point.
  </what-built>
  <how-to-verify>
    1. Run: npx tsc --noEmit — must exit 0 with no output
    2. Run: npm test — must exit 0 (all tests pass)
    3. Run: npm run dev — server must start without errors (watch for MODULE_NOT_FOUND or import errors)
    4. Verify structure: ls src/core/domain/ should show Deal.ts, Tenant.ts, Filter.ts
    5. Verify: ls src/adapters/ should show affiliates/, db/, lock/, publishers/, scrapers/, scheduler/
    6. Verify: ls src/jobs/ should show cronLock.ts, monitorML.ts, monitorPelando.ts
    7. Verify: src/api/, src/content/, src/scraper/, src/telegram/, src/server/ should all be gone (run: ls src/)
    8. Optional Railway check: git push to verify build completes on Railway with new entry point
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any errors found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway deploy | package.json main/start must point to dist/index.js or Railway fails to start after deploy |
| Composition root | src/index.ts boots cron jobs — if any import is missing, jobs silently don't run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | package.json entrypoint mismatch | mitigate | Acceptance criteria verifies main/start/dev all updated; Railway smoke test in checkpoint |
| T-02-08 | Tampering | Internal imports in TelegramPublisher.ts (old relative paths) | mitigate | grep for old path fragments (../api, ../content, ../scraper, ../server) must return 0 |
| T-02-09 | Tampering | Cron job functions not exported from web/server.ts | mitigate | Job files import named exports; tsc --noEmit catches missing exports |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
After all tasks and the human checkpoint:
- npx tsc --noEmit exits 0 (STRUCT-10)
- npm test exits 0
- npm run dev starts successfully (no MODULE_NOT_FOUND)
- src/adapters/publishers/ has TelegramPublisher.ts, WhatsAppPublisher.ts, format.ts (STRUCT-05 complete)
- src/server/, src/telegram/, src/content/, src/api/, src/scraper/ all gone
- Railway deployable: package.json main = "dist/index.js"
</verification>

<success_criteria>
- All 10 STRUCT requirements satisfied (see source audit below)
- tsc --noEmit exits 0 after every commit in this plan
- npm test exits 0
- npm run dev starts the server (human-verified in checkpoint)
- Railway deploy succeeds (human-verified via git push in checkpoint)
- src/ structure matches docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md exactly
</success_criteria>

<output>
Create .planning/phases/02-hexagonal-structure/02-06-SUMMARY.md when done
</output>
