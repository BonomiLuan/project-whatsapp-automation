---
phase: 02-hexagonal-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/domain/Deal.ts
  - src/core/domain/Tenant.ts
  - src/core/domain/Filter.ts
  - src/core/ports/DealScraper.ts
  - src/core/ports/DealPublisher.ts
  - src/core/ports/AffiliateLinkBuilder.ts
  - src/core/ports/DealRepository.ts
  - src/core/ports/TenantRepository.ts
  - src/core/ports/LinkResolver.ts
  - src/core/ports/RotationStore.ts
  - src/core/ports/FeedbackRepository.ts
  - src/core/ports/Scheduler.ts
  - src/core/ports/Lock.ts
  - src/core/usecases/.gitkeep
  - src/adapters/scheduler/NodeCronScheduler.ts
  - src/web/middleware/.gitkeep
  - src/web/routes/api/.gitkeep
  - src/web/routes/redirect/.gitkeep
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - STRUCT-09
  - STRUCT-10

must_haves:
  truths:
    - "src/core/domain/ exists with Deal.ts, Tenant.ts, Filter.ts"
    - "src/core/ports/ exists with all 10 interface files"
    - "src/core/usecases/ exists (empty with .gitkeep)"
    - "src/adapters/scheduler/NodeCronScheduler.ts exists as a typed stub"
    - "tsc --noEmit passes after each commit"
  artifacts:
    - path: "src/core/domain/Deal.ts"
      provides: "Deal type, Marketplace type"
      exports: ["Deal", "Marketplace"]
    - path: "src/core/domain/Tenant.ts"
      provides: "Tenant, Channel, AffiliateConfig types"
      exports: ["Tenant", "Channel", "AffiliateConfig"]
    - path: "src/core/domain/Filter.ts"
      provides: "Filter type"
      exports: ["Filter"]
    - path: "src/core/ports/DealScraper.ts"
      provides: "DealScraper interface"
      exports: ["DealScraper"]
    - path: "src/core/ports/Lock.ts"
      provides: "Lock interface"
      exports: ["Lock"]
    - path: "src/adapters/scheduler/NodeCronScheduler.ts"
      provides: "NodeCronScheduler class implementing Scheduler"
      exports: ["NodeCronScheduler"]
  key_links:
    - from: "src/adapters/scheduler/NodeCronScheduler.ts"
      to: "src/core/ports/Scheduler.ts"
      via: "implements Scheduler"
      pattern: "implements Scheduler"
---

<objective>
Create the core hexagonal skeleton: domain types, port interfaces, and directory structure. All new files; zero existing files modified. tsc must pass after each commit.

Purpose: Establish the target folder structure and contracts that all subsequent moves will plug into. No behavior changes — pure additive.
Output: src/core/ (domain + ports + usecases stub), src/adapters/scheduler/NodeCronScheduler.ts, src/web/ skeleton directories.
</objective>

<execution_context>
@/Users/bonomiluan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bonomiluan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hexagonal-structure/02-CONTEXT.md
@.planning/phases/02-hexagonal-structure/02-RESEARCH.md
@docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md
</context>

<interfaces>
<!-- All types and signatures below come verbatim from docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md -->
<!-- Executor: reproduce these exactly — the spec is the sole source of truth (D-02b) -->

Domain types to create:

src/core/domain/Deal.ts:
  type Marketplace = 'pelando' | 'mercadolivre' | 'shopee' | 'amazon'
  type Deal = { id, title, price, originalPrice?, url, imageUrl?, couponCode?, marketplace: Marketplace, category, postedAt: Date }

src/core/domain/Filter.ts:
  type Filter = { keywords, excludeKeywords, minDiscount, categories }

src/core/domain/Tenant.ts:
  type Channel = { type: 'telegram' | 'whatsapp', channelId }
  type AffiliateConfig = { amazonTag?, mlAppId?, shopeeSourceId? }
  type Tenant = { id, name, channels: Channel[], filters: Filter, affiliates: AffiliateConfig, active }

Port interfaces to create (all 10):
  DealScraper:         fetchDeals(category?: string): Promise<Deal[]>
  DealPublisher:       publish(deal: Deal, tenant: Tenant): Promise<void>
  AffiliateLinkBuilder: supports(marketplace: Marketplace): boolean  |  build(deal: Deal, config: AffiliateConfig): Promise<Deal>
  DealRepository:      findRecentlySentIds(tenantId: string, withinDays: number): Promise<Set<string>>  |  markAsSent(dealId: string, tenantId: string): Promise<void>
  TenantRepository:    findAll(): Promise<Tenant[]>  |  findById(id: string): Promise<Tenant | null>  |  save(tenant: Tenant): Promise<void>
  LinkResolver:        resolve(code: string): Promise<string | null>  |  create(url: string, meta: Record<string, string>): Promise<string>
  RotationStore:       load(tenantId: string): Promise<RotationCursor>  |  save(tenantId: string, cursor: RotationCursor): Promise<void>  |  reset(tenantId: string): Promise<void>
  FeedbackRepository:  record(deal: Deal, tenantId: string, userId: string, reaction: 'like' | 'dislike'): Promise<void>  |  statsByCategory(tenantId: string): Promise<CategoryStats>
  Scheduler:           schedule(name: string, cron: string, job: () => Promise<void>): void
  Lock:                withLock<T>(key: string, fn: () => Promise<T>): Promise<T | null>

Supporting types needed in port files:
  RotationCursor = { roundRobinIndex: number; lastSource: string | null }
  CategoryStats = Record<string, { likes: number; dislikes: number }>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create directory scaffold and domain types</name>
  <files>
    src/core/domain/Deal.ts
    src/core/domain/Filter.ts
    src/core/domain/Tenant.ts
    src/core/usecases/.gitkeep
    src/web/middleware/.gitkeep
    src/web/routes/api/.gitkeep
    src/web/routes/redirect/.gitkeep
  </files>
  <read_first>
    docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md — section "Core Domain > Entidades" for exact type shapes
  </read_first>
  <action>
    Create directories: src/core/domain/, src/core/ports/, src/core/usecases/, src/adapters/scrapers/, src/adapters/publishers/, src/adapters/affiliates/, src/adapters/db/, src/adapters/lock/, src/adapters/scheduler/, src/web/middleware/, src/web/routes/api/, src/web/routes/redirect/, src/jobs/.

    Create src/core/usecases/.gitkeep, src/web/middleware/.gitkeep, src/web/routes/api/.gitkeep, src/web/routes/redirect/.gitkeep as empty files.

    Create src/core/domain/Deal.ts — export type Marketplace and type Deal exactly as defined in the spec "Core Domain > Entidades" section. Use TypeScript export keyword on each type.

    Create src/core/domain/Filter.ts — export type Filter exactly as in the spec.

    Create src/core/domain/Tenant.ts — export type Channel, type AffiliateConfig, type Tenant exactly as in the spec. Tenant.filters must be typed as Filter (import from ./Deal is not needed; Filter is a separate file — import from ./Filter.js).

    Per D-02b: zero deviation from spec signatures.

    Run tsc --noEmit. If it fails, fix before committing. Commit message: "feat(02): create src/core directory scaffold + domain types".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/core/domain/Deal.ts exists and exports type Marketplace and type Deal
    - src/core/domain/Filter.ts exists and exports type Filter
    - src/core/domain/Tenant.ts exists and exports type Channel, AffiliateConfig, Tenant
    - src/core/usecases/.gitkeep exists
    - src/web/middleware/.gitkeep exists
    - src/web/routes/api/.gitkeep exists
    - src/web/routes/redirect/.gitkeep exists
    - npx tsc --noEmit exits 0 with no errors
  </acceptance_criteria>
  <done>Domain types created, directories scaffolded, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 10 port interfaces</name>
  <files>
    src/core/ports/DealScraper.ts
    src/core/ports/DealPublisher.ts
    src/core/ports/AffiliateLinkBuilder.ts
    src/core/ports/DealRepository.ts
    src/core/ports/TenantRepository.ts
    src/core/ports/LinkResolver.ts
    src/core/ports/RotationStore.ts
    src/core/ports/FeedbackRepository.ts
    src/core/ports/Scheduler.ts
    src/core/ports/Lock.ts
  </files>
  <read_first>
    docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md — section "Ports (Interfaces)" and the RotationStore / FeedbackRepository definitions
    src/core/domain/Deal.ts (created in Task 1) — for import paths
    src/core/domain/Tenant.ts (created in Task 1)
    src/core/domain/Filter.ts (created in Task 1)
  </read_first>
  <action>
    Create each of the 10 interface files in src/core/ports/. Each file contains exactly one interface (or supporting type + interface). Import domain types using .js extension (per TypeScript ESM convention already in use in the project).

    DealScraper.ts: imports Deal from ../domain/Deal.js; exports interface DealScraper with fetchDeals(category?: string): Promise&lt;Deal[]&gt;
    DealPublisher.ts: imports Deal, Tenant; exports interface DealPublisher with publish(deal: Deal, tenant: Tenant): Promise&lt;void&gt;
    AffiliateLinkBuilder.ts: imports Deal, Marketplace (from Deal.ts), AffiliateConfig (from Tenant.ts); exports interface AffiliateLinkBuilder with supports(marketplace: Marketplace): boolean and build(deal: Deal, config: AffiliateConfig): Promise&lt;Deal&gt;
    DealRepository.ts: exports interface DealRepository with findRecentlySentIds(tenantId: string, withinDays: number): Promise&lt;Set&lt;string&gt;&gt; and markAsSent(dealId: string, tenantId: string): Promise&lt;void&gt;
    TenantRepository.ts: imports Tenant; exports interface TenantRepository with findAll(): Promise&lt;Tenant[]&gt;, findById(id: string): Promise&lt;Tenant | null&gt;, save(tenant: Tenant): Promise&lt;void&gt;
    LinkResolver.ts: exports interface LinkResolver with resolve(code: string): Promise&lt;string | null&gt; and create(url: string, meta: Record&lt;string, string&gt;): Promise&lt;string&gt;
    RotationStore.ts: exports type RotationCursor = { roundRobinIndex: number; lastSource: string | null } and interface RotationStore with load(tenantId: string): Promise&lt;RotationCursor&gt;, save(tenantId: string, cursor: RotationCursor): Promise&lt;void&gt;, reset(tenantId: string): Promise&lt;void&gt;
    FeedbackRepository.ts: imports Deal; exports type CategoryStats = Record&lt;string, { likes: number; dislikes: number }&gt; and interface FeedbackRepository with record(deal: Deal, tenantId: string, userId: string, reaction: 'like' | 'dislike'): Promise&lt;void&gt; and statsByCategory(tenantId: string): Promise&lt;CategoryStats&gt;
    Scheduler.ts: exports interface Scheduler with schedule(name: string, cron: string, job: () =&gt; Promise&lt;void&gt;): void
    Lock.ts: exports interface Lock with withLock&lt;T&gt;(key: string, fn: () =&gt; Promise&lt;T&gt;): Promise&lt;T | null&gt;

    Per D-02c: all 10 interfaces must be created. Per D-02b: method signatures must match spec exactly.

    Run tsc --noEmit. Fix any errors before committing. Commit message: "feat(02): create 10 port interfaces in src/core/ports/".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files exist: src/core/ports/{DealScraper,DealPublisher,AffiliateLinkBuilder,DealRepository,TenantRepository,LinkResolver,RotationStore,FeedbackRepository,Scheduler,Lock}.ts
    - grep -c "interface DealScraper" src/core/ports/DealScraper.ts returns 1
    - grep -c "interface Lock" src/core/ports/Lock.ts returns 1
    - grep "withLock" src/core/ports/Lock.ts contains "Promise&lt;T | null&gt;"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All 10 port interfaces created per spec, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create NodeCronScheduler stub</name>
  <files>
    src/adapters/scheduler/NodeCronScheduler.ts
  </files>
  <read_first>
    src/core/ports/Scheduler.ts (created in Task 2) — for the Scheduler interface signature
    .planning/phases/02-hexagonal-structure/02-RESEARCH.md — section "Code Examples > NodeCronScheduler Stub"
  </read_first>
  <action>
    Create src/adapters/scheduler/NodeCronScheduler.ts as a typed stub that implements the Scheduler port.

    The file must: import cron from 'node-cron'; import type Scheduler from ../../core/ports/Scheduler.js; export class NodeCronScheduler implements Scheduler with a single method schedule(name: string, expression: string, job: () =&gt; Promise&lt;void&gt;): void that calls cron.schedule(expression, job).

    The name parameter is accepted but not used in Phase 2 (logging/tracing comes in Phase 3).

    Per CONTEXT.md Deferred: NodeCronScheduler is a stub only — no wiring to the composition root yet.

    Run tsc --noEmit. Fix errors. Commit message: "feat(02): add NodeCronScheduler stub in src/adapters/scheduler/".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/scheduler/NodeCronScheduler.ts exists
    - grep "implements Scheduler" src/adapters/scheduler/NodeCronScheduler.ts returns a match
    - grep "class NodeCronScheduler" src/adapters/scheduler/NodeCronScheduler.ts returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>NodeCronScheduler stub created, implements Scheduler, tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Phase 2 plan 01 creates new files only — no I/O, no external calls, no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Port interface signatures | mitigate | Method signatures validated against spec verbatim; tsc --noEmit catches type mismatches |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 |
</threat_model>

<verification>
After all 3 tasks:
- npx tsc --noEmit exits 0 (STRUCT-10)
- src/core/domain/ has 3 files (STRUCT-01, STRUCT-02)
- src/core/ports/ has 10 files (STRUCT-01, STRUCT-03)
- src/core/usecases/.gitkeep exists (STRUCT-01)
- src/adapters/scheduler/NodeCronScheduler.ts implements Scheduler (STRUCT-09)
- npm test passes (existing tests unaffected — no production files modified)
</verification>

<success_criteria>
- 14 new files created, 0 existing files modified
- tsc --noEmit exits 0 after each of the 3 commits
- npm test passes (vitest exits 0) — existing tests unaffected
- All type signatures match docs/superpowers/specs/2026-06-16-hexagonal-architecture-design.md exactly
</success_criteria>

<output>
Create .planning/phases/02-hexagonal-structure/02-01-SUMMARY.md when done
</output>
