---
phase: 02-hexagonal-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/adapters/affiliates/AmazonAffiliate.ts
  - src/adapters/affiliates/MLAffiliate.ts
  - src/adapters/affiliates/ShopeeAffiliate.ts
  - src/adapters/db/HistoryRepository.ts
  - src/content/pelando.ts
  - src/content/mercadoLivre.ts
  - src/telegram/bot.ts
  - src/server/index.ts
autonomous: true
requirements:
  - STRUCT-06
  - STRUCT-10

must_haves:
  truths:
    - "src/adapters/affiliates/AmazonAffiliate.ts exists with full content of src/api/amazonAffiliate.ts"
    - "src/adapters/affiliates/MLAffiliate.ts exists with full content of src/api/mercadoLivreAffiliate.ts"
    - "src/adapters/affiliates/ShopeeAffiliate.ts exists with full content of src/api/shopeeAffiliate.ts"
    - "src/adapters/db/HistoryRepository.ts exists with full content of src/server/history.ts"
    - "src/api/ and old history.ts are deleted"
    - "tsc --noEmit passes after each individual commit"
  artifacts:
    - path: "src/adapters/affiliates/AmazonAffiliate.ts"
      provides: "Amazon affiliate link builder (moved)"
    - path: "src/adapters/affiliates/MLAffiliate.ts"
      provides: "Mercado Livre affiliate (moved)"
    - path: "src/adapters/affiliates/ShopeeAffiliate.ts"
      provides: "Shopee affiliate (moved)"
    - path: "src/adapters/db/HistoryRepository.ts"
      provides: "Deal send history persistence (moved)"
  key_links:
    - from: "src/content/pelando.ts"
      to: "src/adapters/affiliates/AmazonAffiliate.ts"
      via: "import updated"
      pattern: "adapters/affiliates/AmazonAffiliate"
    - from: "src/server/index.ts"
      to: "src/adapters/affiliates/ShopeeAffiliate.ts"
      via: "import updated"
      pattern: "adapters/affiliates/ShopeeAffiliate"
---

<objective>
Move the 3 affiliate adapters (leaf nodes — only import external packages) and history.ts (also a leaf) to their hexagonal locations. Each move is one atomic commit: new file + all importers updated + tsc passes.

Purpose: Remove src/api/ and clean up history from src/server/, establishing src/adapters/affiliates/ and adding to src/adapters/db/.
Output: 4 files at new paths; src/api/ directory removed; all importers updated.
</objective>

<execution_context>
@/Users/bonomiluan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bonomiluan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hexagonal-structure/02-CONTEXT.md
@.planning/phases/02-hexagonal-structure/02-RESEARCH.md
</context>

<interfaces>
<!-- Import graph verified in RESEARCH.md — importers per file: -->

AmazonAffiliate importers (files to update in same commit):
  src/content/pelando.ts — changes '../api/amazonAffiliate.js' → '../adapters/affiliates/AmazonAffiliate.js'
  src/telegram/bot.ts   — changes '../api/amazonAffiliate.js' → '../adapters/affiliates/AmazonAffiliate.js'

MLAffiliate importers:
  src/content/pelando.ts       — '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'
  src/content/mercadoLivre.ts  — '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'
  src/telegram/bot.ts          — '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'

ShopeeAffiliate importers:
  src/content/pelando.ts       — '../api/shopeeAffiliate.js' → '../adapters/affiliates/ShopeeAffiliate.js'
  src/content/mercadoLivre.ts  — '../api/shopeeAffiliate.js' (type) → '../adapters/affiliates/ShopeeAffiliate.js'
  src/telegram/bot.ts          — '../api/shopeeAffiliate.js' → '../adapters/affiliates/ShopeeAffiliate.js'
  src/server/index.ts          — '../api/shopeeAffiliate.js' → './adapters/affiliates/ShopeeAffiliate.js' (from server/)

HistoryRepository importers:
  src/server/index.ts — './history.js' → '../adapters/db/HistoryRepository.js' (from web/ eventually, but now still at server/index.ts)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move AmazonAffiliate (Commit B1)</name>
  <files>
    src/adapters/affiliates/AmazonAffiliate.ts
    src/content/pelando.ts
    src/telegram/bot.ts
  </files>
  <read_first>
    src/api/amazonAffiliate.ts — full file content (copy to new path)
    src/content/pelando.ts — find import line for amazonAffiliate to update
    src/telegram/bot.ts — find import line for amazonAffiliate to update
  </read_first>
  <action>
    Copy src/api/amazonAffiliate.ts to src/adapters/affiliates/AmazonAffiliate.ts — identical content, zero changes to the file body.

    In src/content/pelando.ts: change the import path from '../api/amazonAffiliate.js' to '../adapters/affiliates/AmazonAffiliate.js'. No other changes to pelando.ts.

    In src/telegram/bot.ts: change the import path from '../api/amazonAffiliate.js' to '../adapters/affiliates/AmazonAffiliate.js'. No other changes to bot.ts.

    Delete src/api/amazonAffiliate.ts.

    Run npx tsc --noEmit. Must exit 0. Fix any errors before committing.

    Commit all 4 file changes (new file + 2 updated importers + 1 deletion) as one atomic commit. Commit message: "refactor(02): move amazonAffiliate → adapters/affiliates/AmazonAffiliate (B1)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/affiliates/AmazonAffiliate.ts exists
    - src/api/amazonAffiliate.ts does NOT exist
    - grep "adapters/affiliates/AmazonAffiliate" src/content/pelando.ts returns a match
    - grep "adapters/affiliates/AmazonAffiliate" src/telegram/bot.ts returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AmazonAffiliate at new path, importers updated, old file deleted, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Move MLAffiliate and ShopeeAffiliate (Commits B2 + B3)</name>
  <files>
    src/adapters/affiliates/MLAffiliate.ts
    src/adapters/affiliates/ShopeeAffiliate.ts
    src/content/pelando.ts
    src/content/mercadoLivre.ts
    src/telegram/bot.ts
    src/server/index.ts
  </files>
  <read_first>
    src/api/mercadoLivreAffiliate.ts — full content
    src/api/shopeeAffiliate.ts — full content
    src/content/pelando.ts — lines with mercadoLivreAffiliate and shopeeAffiliate imports
    src/content/mercadoLivre.ts — lines with mercadoLivreAffiliate and shopeeAffiliate imports
    src/telegram/bot.ts — lines with mercadoLivreAffiliate and shopeeAffiliate imports
    src/server/index.ts — line with shopeeAffiliate import (from RESEARCH.md: line 14)
  </read_first>
  <action>
    COMMIT B2 — MLAffiliate:
    Copy src/api/mercadoLivreAffiliate.ts to src/adapters/affiliates/MLAffiliate.ts (identical content).
    Update import in src/content/pelando.ts: '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'.
    Update import in src/content/mercadoLivre.ts: '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'.
    Update import in src/telegram/bot.ts: '../api/mercadoLivreAffiliate.js' → '../adapters/affiliates/MLAffiliate.js'.
    Delete src/api/mercadoLivreAffiliate.ts.
    Run npx tsc --noEmit. Must exit 0.
    Commit with message: "refactor(02): move mercadoLivreAffiliate → adapters/affiliates/MLAffiliate (B2)".

    COMMIT B3 — ShopeeAffiliate:
    Copy src/api/shopeeAffiliate.ts to src/adapters/affiliates/ShopeeAffiliate.ts (identical content).
    Update import in src/content/pelando.ts: '../api/shopeeAffiliate.js' → '../adapters/affiliates/ShopeeAffiliate.js'.
    Update import in src/content/mercadoLivre.ts: '../api/shopeeAffiliate.js' → '../adapters/affiliates/ShopeeAffiliate.js'.
    Update import in src/telegram/bot.ts: '../api/shopeeAffiliate.js' → '../adapters/affiliates/ShopeeAffiliate.js'.
    Update import in src/server/index.ts: the shopeeAffiliate import path. The current path in index.ts is '../api/shopeeAffiliate.js' — change to '../adapters/affiliates/ShopeeAffiliate.js'. (Note: index.ts is at src/server/index.ts so relative path from there is '../adapters/affiliates/ShopeeAffiliate.js'.)
    Delete src/api/shopeeAffiliate.ts.
    Run npx tsc --noEmit. Must exit 0.
    Commit with message: "refactor(02): move shopeeAffiliate → adapters/affiliates/ShopeeAffiliate (B3)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/affiliates/MLAffiliate.ts exists
    - src/adapters/affiliates/ShopeeAffiliate.ts exists
    - src/api/mercadoLivreAffiliate.ts does NOT exist
    - src/api/shopeeAffiliate.ts does NOT exist
    - grep "adapters/affiliates/MLAffiliate" src/content/pelando.ts returns a match
    - grep "adapters/affiliates/ShopeeAffiliate" src/server/index.ts returns a match
    - npx tsc --noEmit exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>MLAffiliate and ShopeeAffiliate at new paths, all importers updated, old files deleted, tsc and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Move HistoryRepository (Commit B4)</name>
  <files>
    src/adapters/db/HistoryRepository.ts
    src/server/index.ts
  </files>
  <read_first>
    src/server/history.ts — full content
    src/server/index.ts — line with history import (from RESEARCH.md: line 12, './history.js')
  </read_first>
  <action>
    Copy src/server/history.ts to src/adapters/db/HistoryRepository.ts (identical content).

    In src/server/index.ts: update the import of history from './history.js' to '../adapters/db/HistoryRepository.js'. (index.ts is at src/server/, so relative path to adapters/db is '../adapters/db/HistoryRepository.js'.)

    Delete src/server/history.ts.

    Run npx tsc --noEmit. Must exit 0. Fix any errors.

    Commit: "refactor(02): move server/history.ts → adapters/db/HistoryRepository (B4)".

    After commit: verify src/api/ directory is now empty (all 3 files moved). If empty, remove the directory: rmdir src/api/ (or leave it — TypeScript won't care about empty dirs, but clean up is tidier). Commit the rmdir as part of the B4 commit or separately.
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/db/HistoryRepository.ts exists
    - src/server/history.ts does NOT exist
    - grep "adapters/db/HistoryRepository" src/server/index.ts returns a match
    - src/api/ directory does not exist (all 3 affiliate files moved in Tasks 1-2)
    - npx tsc --noEmit exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>HistoryRepository at new path, index.ts import updated, src/api/ cleaned up, tsc and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure file moves; no new I/O, no new entry points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Import paths in pelando.ts, bot.ts, index.ts | mitigate | tsc --noEmit catches stale import paths after each commit |
| T-02-SC | Tampering | npm installs | accept | No new packages; no install step |
</threat_model>

<verification>
After all 3 tasks and 4 commits:
- npx tsc --noEmit exits 0 (STRUCT-10)
- npm test exits 0 (regression check)
- src/adapters/affiliates/ contains AmazonAffiliate.ts, MLAffiliate.ts, ShopeeAffiliate.ts (STRUCT-06)
- src/adapters/db/HistoryRepository.ts exists
- src/api/ directory is gone
- src/server/history.ts is gone
</verification>

<success_criteria>
- 4 atomic commits, each passing tsc --noEmit before the next begins
- npm test green after wave completes
- src/api/ directory removed from repository
- No behavior changes — file contents identical to originals
</success_criteria>

<output>
Create .planning/phases/02-hexagonal-structure/02-02-SUMMARY.md when done
</output>
