---
phase: 02-hexagonal-structure
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/adapters/publishers/format.ts
  - src/adapters/publishers/WhatsAppPublisher.ts
  - src/adapters/scrapers/ProductScraper.ts
  - src/server/index.ts
  - src/telegram/bot.ts
autonomous: true
requirements:
  - STRUCT-05
  - STRUCT-10

must_haves:
  truths:
    - "src/adapters/publishers/format.ts exists (moved from content/messageBuilder.ts)"
    - "src/adapters/publishers/WhatsAppPublisher.ts exists (moved from api/metaClient.ts)"
    - "src/adapters/scrapers/ProductScraper.ts exists (moved from scraper/productScraper.ts)"
    - "All importers updated; old paths deleted"
    - "tsc --noEmit passes after each commit"
  artifacts:
    - path: "src/adapters/publishers/format.ts"
      provides: "Message formatting helpers (moved)"
    - path: "src/adapters/publishers/WhatsAppPublisher.ts"
      provides: "Meta/WhatsApp API client (moved)"
    - path: "src/adapters/scrapers/ProductScraper.ts"
      provides: "Generic product scraper (moved)"
  key_links:
    - from: "src/server/index.ts"
      to: "src/adapters/publishers/format.ts"
      via: "import updated"
      pattern: "adapters/publishers/format"
    - from: "src/telegram/bot.ts"
      to: "src/adapters/scrapers/ProductScraper.ts"
      via: "import updated"
      pattern: "adapters/scrapers/ProductScraper"
---

<objective>
Move messageBuilder.ts (→ format.ts), metaClient.ts (→ WhatsAppPublisher.ts), and productScraper.ts (→ ProductScraper.ts) to their hexagonal adapter locations. Each move is one atomic commit per D-03.

Purpose: Establish src/adapters/publishers/ with formatting and WhatsApp client, and src/adapters/scrapers/ with the generic scraper. Prerequisite for Wave 4 (scraper moves that depend on these).
Output: 3 files at new paths; src/content/messageBuilder.ts, src/api/metaClient.ts, src/scraper/productScraper.ts deleted.
</objective>

<execution_context>
@/Users/bonomiluan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bonomiluan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hexagonal-structure/02-CONTEXT.md
@.planning/phases/02-hexagonal-structure/02-RESEARCH.md
</context>

<interfaces>
<!-- Import graph verified in RESEARCH.md -->

messageBuilder.ts importers (3 files):
  src/server/index.ts  — '../content/messageBuilder.js' → '../adapters/publishers/format.js'
  src/telegram/bot.ts  — '../content/messageBuilder.js' → '../adapters/publishers/format.js'
  src/api/metaClient.ts — imports MessagePayload type from '../content/messageBuilder.js'
    NOTE: metaClient.ts itself moves in C2. Update its import BEFORE moving it, or in the same commit as messageBuilder.ts move.
    RECOMMENDED: Move messageBuilder.ts first (C1), update metaClient.ts in same commit, then move metaClient.ts (C2).

metaClient.ts importers (after messageBuilder move):
  src/server/index.ts — '../api/metaClient.js' → '../adapters/publishers/WhatsAppPublisher.js'
  src/telegram/bot.ts — '../api/metaClient.js' → '../adapters/publishers/WhatsAppPublisher.js'

productScraper.ts importers (2 files):
  src/server/index.ts  — '../scraper/productScraper.js' → '../adapters/scrapers/ProductScraper.js'
  src/telegram/bot.ts  — '../scraper/productScraper.js' → '../adapters/scrapers/ProductScraper.js'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move messageBuilder.ts → format.ts (Commit C1)</name>
  <files>
    src/adapters/publishers/format.ts
    src/server/index.ts
    src/telegram/bot.ts
    src/api/metaClient.ts
  </files>
  <read_first>
    src/content/messageBuilder.ts — full content (this is what gets copied)
    src/server/index.ts — line with messageBuilder import (RESEARCH.md: line 10)
    src/telegram/bot.ts — line with messageBuilder import (RESEARCH.md: line 7)
    src/api/metaClient.ts — line with messageBuilder import (type-only import of MessagePayload)
  </read_first>
  <action>
    Copy src/content/messageBuilder.ts to src/adapters/publishers/format.ts (identical content — zero changes to file body).

    In src/server/index.ts: change '../content/messageBuilder.js' to '../adapters/publishers/format.js'.
    In src/telegram/bot.ts: change '../content/messageBuilder.js' to '../adapters/publishers/format.js'.
    In src/api/metaClient.ts: change '../content/messageBuilder.js' to '../adapters/publishers/format.js' (this updates metaClient.ts in-place before its own move in Task 2).

    Delete src/content/messageBuilder.ts.

    Run npx tsc --noEmit. Must exit 0. Fix any errors.

    Commit: "refactor(02): move content/messageBuilder.ts → adapters/publishers/format (C1)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/publishers/format.ts exists
    - src/content/messageBuilder.ts does NOT exist
    - grep "adapters/publishers/format" src/server/index.ts returns a match
    - grep "adapters/publishers/format" src/telegram/bot.ts returns a match
    - grep "adapters/publishers/format" src/api/metaClient.ts returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>format.ts at new path, 3 importers updated, old file deleted, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Move metaClient.ts → WhatsAppPublisher.ts (Commit C2)</name>
  <files>
    src/adapters/publishers/WhatsAppPublisher.ts
    src/server/index.ts
    src/telegram/bot.ts
  </files>
  <read_first>
    src/api/metaClient.ts — full content (already updated to import from adapters/publishers/format in Task 1)
    src/server/index.ts — line with metaClient import (RESEARCH.md: line 11)
    src/telegram/bot.ts — line with metaClient import (RESEARCH.md: line 8)
  </read_first>
  <action>
    Copy src/api/metaClient.ts to src/adapters/publishers/WhatsAppPublisher.ts (identical content — the format.ts import is already updated from Task 1).

    In src/server/index.ts: change '../api/metaClient.js' to '../adapters/publishers/WhatsAppPublisher.js'.
    In src/telegram/bot.ts: change '../api/metaClient.js' to '../adapters/publishers/WhatsAppPublisher.js'.

    Delete src/api/metaClient.ts.

    Run npx tsc --noEmit. Must exit 0.

    Commit: "refactor(02): move api/metaClient.ts → adapters/publishers/WhatsAppPublisher (C2)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/publishers/WhatsAppPublisher.ts exists
    - src/api/metaClient.ts does NOT exist
    - grep "adapters/publishers/WhatsAppPublisher" src/server/index.ts returns a match
    - grep "adapters/publishers/WhatsAppPublisher" src/telegram/bot.ts returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>WhatsAppPublisher.ts at new path, importers updated, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Move productScraper.ts → ProductScraper.ts (Commit C3)</name>
  <files>
    src/adapters/scrapers/ProductScraper.ts
    src/server/index.ts
    src/telegram/bot.ts
  </files>
  <read_first>
    src/scraper/productScraper.ts — full content
    src/server/index.ts — lines with productScraper imports (RESEARCH.md: lines 9 and 16 — may be two import references)
    src/telegram/bot.ts — line with productScraper import (RESEARCH.md: line 6)
  </read_first>
  <action>
    Copy src/scraper/productScraper.ts to src/adapters/scrapers/ProductScraper.ts (identical content).

    In src/server/index.ts: change ALL occurrences of '../scraper/productScraper.js' to '../adapters/scrapers/ProductScraper.js'. RESEARCH.md notes lines 9 and 16 both import from productScraper — update both.
    In src/telegram/bot.ts: change '../scraper/productScraper.js' to '../adapters/scrapers/ProductScraper.js'.

    Delete src/scraper/productScraper.ts.
    Note: src/scraper/test.ts (if it exists) has a package.json script pointing to it. Do not delete test.ts — leave it in place or move to src/adapters/scrapers/test.ts and update the "scripts.scrape:test" in package.json. Check if test.ts exists first: if yes, either leave it or update package.json script. Either choice is acceptable; tsc must pass.

    Run npx tsc --noEmit. Must exit 0. Run npm test. Must exit 0.

    Commit: "refactor(02): move scraper/productScraper.ts → adapters/scrapers/ProductScraper (C3)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/scrapers/ProductScraper.ts exists
    - src/scraper/productScraper.ts does NOT exist
    - grep "adapters/scrapers/ProductScraper" src/server/index.ts returns 2 matches (or at least 1 if only one import reference exists)
    - grep "adapters/scrapers/ProductScraper" src/telegram/bot.ts returns a match
    - npx tsc --noEmit exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>ProductScraper.ts at new path, all importers updated, tsc and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure file moves; no new I/O or entry points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Import paths across 3 files per commit | mitigate | tsc --noEmit after each commit is mandatory per D-03b |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
After all 3 tasks and 3 commits:
- npx tsc --noEmit exits 0 (STRUCT-10)
- npm test exits 0
- src/adapters/publishers/ has format.ts and WhatsAppPublisher.ts (STRUCT-05 partial — TelegramPublisher comes in plan 05)
- src/adapters/scrapers/ has ProductScraper.ts (STRUCT-04 partial)
- src/content/messageBuilder.ts gone
- src/api/metaClient.ts gone
- src/scraper/productScraper.ts gone
</verification>

<success_criteria>
- 3 atomic commits (C1, C2, C3), each compilable
- npm test green after wave
- No behavior changes — file contents identical to originals (except metaClient.ts which had its messageBuilder import updated in Task 1)
</success_criteria>

<output>
Create .planning/phases/02-hexagonal-structure/02-03-SUMMARY.md when done
</output>
