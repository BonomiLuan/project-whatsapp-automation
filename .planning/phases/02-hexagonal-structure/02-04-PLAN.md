---
phase: 02-hexagonal-structure
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/adapters/scrapers/PelandoScraper.ts
  - src/adapters/scrapers/MercadoLivreScraper.ts
  - src/server/index.ts
autonomous: true
requirements:
  - STRUCT-04
  - STRUCT-10

must_haves:
  truths:
    - "src/adapters/scrapers/PelandoScraper.ts exists (moved from content/pelando.ts)"
    - "src/adapters/scrapers/MercadoLivreScraper.ts exists (moved from content/mercadoLivre.ts)"
    - "src/content/ directory is gone"
    - "Dynamic import string in server/index.ts manually updated to point to MercadoLivreScraper.js"
    - "tsc --noEmit passes after each commit"
  artifacts:
    - path: "src/adapters/scrapers/PelandoScraper.ts"
      provides: "Pelando deal scraper (moved)"
    - path: "src/adapters/scrapers/MercadoLivreScraper.ts"
      provides: "Mercado Livre scraper (moved)"
  key_links:
    - from: "src/server/index.ts"
      to: "src/adapters/scrapers/PelandoScraper.ts"
      via: "static import updated"
      pattern: "adapters/scrapers/PelandoScraper"
    - from: "src/server/index.ts"
      to: "src/adapters/scrapers/MercadoLivreScraper.ts"
      via: "dynamic import string updated"
      pattern: "adapters/scrapers/MercadoLivreScraper"
---

<objective>
Move the two content scrapers (PelandoScraper and MercadoLivreScraper) to src/adapters/scrapers/. Critical: mercadoLivre.ts has a dynamic import in server/index.ts (line 310) that TypeScript does NOT catch — must be updated manually.

Purpose: Complete src/adapters/scrapers/ and eliminate src/content/ directory.
Output: PelandoScraper.ts and MercadoLivreScraper.ts at new paths; src/content/ removed.
</objective>

<execution_context>
@/Users/bonomiluan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bonomiluan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hexagonal-structure/02-CONTEXT.md
@.planning/phases/02-hexagonal-structure/02-RESEARCH.md
</context>

<interfaces>
<!-- From RESEARCH.md verified import graph -->

pelando.ts importers:
  src/server/index.ts — '../content/pelando.js' → '../adapters/scrapers/PelandoScraper.js'
  NOTE: By the time this plan runs, pelando.ts already has updated imports for AmazonAffiliate, MLAffiliate, ShopeeAffiliate from plan 02-02.

mercadoLivre.ts importers:
  src/server/index.ts — STATIC import (RESEARCH.md) updated to '../adapters/scrapers/MercadoLivreScraper.js'
  src/server/index.ts LINE 310 — DYNAMIC import: await import('../content/mercadoLivre.js')
    THIS IS NOT CAUGHT BY tsc --noEmit. Must be updated manually to:
    await import('../adapters/scrapers/MercadoLivreScraper.js')
    CRITICAL: Forgetting this causes a silent runtime crash (MODULE_NOT_FOUND) even though tsc passes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move pelando.ts → PelandoScraper.ts (Commit D1)</name>
  <files>
    src/adapters/scrapers/PelandoScraper.ts
    src/server/index.ts
  </files>
  <read_first>
    src/content/pelando.ts — full content (already has updated imports for affiliates from plan 02-02)
    src/server/index.ts — line with pelando import (RESEARCH.md: line 13, '../content/pelando.js')
  </read_first>
  <action>
    Copy src/content/pelando.ts to src/adapters/scrapers/PelandoScraper.ts — identical content (the internal affiliate imports are already at the new paths from plan 02-02).

    In src/server/index.ts: change the static import of '../content/pelando.js' to '../adapters/scrapers/PelandoScraper.js'.

    Delete src/content/pelando.ts.

    Run npx tsc --noEmit. Must exit 0.

    Commit: "refactor(02): move content/pelando.ts → adapters/scrapers/PelandoScraper (D1)".
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/scrapers/PelandoScraper.ts exists
    - src/content/pelando.ts does NOT exist
    - grep "adapters/scrapers/PelandoScraper" src/server/index.ts returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PelandoScraper.ts at new path, index.ts import updated, tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Move mercadoLivre.ts → MercadoLivreScraper.ts with dynamic import fix (Commit D2)</name>
  <files>
    src/adapters/scrapers/MercadoLivreScraper.ts
    src/server/index.ts
  </files>
  <read_first>
    src/content/mercadoLivre.ts — full content (already has updated imports for affiliates from plan 02-02)
    src/server/index.ts — ALL references to mercadoLivre: static import AND line 310 dynamic import
  </read_first>
  <action>
    Copy src/content/mercadoLivre.ts to src/adapters/scrapers/MercadoLivreScraper.ts — identical content.

    In src/server/index.ts, update TWO references:
    1. STATIC import: change '../content/mercadoLivre.js' to '../adapters/scrapers/MercadoLivreScraper.js'.
    2. DYNAMIC import at line ~310: change await import('../content/mercadoLivre.js') to await import('../adapters/scrapers/MercadoLivreScraper.js'). This is a string literal — tsc will NOT catch it if missed. Search for the string 'content/mercadoLivre' in index.ts and replace all occurrences.

    Delete src/content/mercadoLivre.ts.

    CRITICAL VERIFICATION: After editing, run grep "content/mercadoLivre" src/server/index.ts — this must return zero matches. If any remain, the dynamic import was not updated and will crash at runtime.

    Run npx tsc --noEmit. Must exit 0. Run npm test. Must exit 0.

    After all verifications pass: commit all changes (new file + updated index.ts + deleted old file).
    Commit message: "refactor(02): move content/mercadoLivre.ts → adapters/scrapers/MercadoLivreScraper (D2)".

    src/content/ should now be empty. Remove it: rmdir src/content/ (or git tracks deletions; the directory disappears automatically when all files are removed).
  </action>
  <verify>
    <automated>cd /Users/bonomiluan/repository/api-whatsapp && grep -c "content/mercadoLivre" src/server/index.ts || true; npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/scrapers/MercadoLivreScraper.ts exists
    - src/content/mercadoLivre.ts does NOT exist
    - src/content/ directory is gone
    - grep "content/mercadoLivre" src/server/index.ts returns ZERO matches (both static and dynamic import updated)
    - grep "adapters/scrapers/MercadoLivreScraper" src/server/index.ts returns at least 2 matches (static + dynamic)
    - npx tsc --noEmit exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>MercadoLivreScraper.ts at new path, both static and dynamic imports in index.ts updated, src/content/ removed, tsc and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dynamic import risk | await import() string at index.ts:310 not type-checked — silent runtime failure if stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Dynamic import string in server/index.ts | mitigate | grep "content/mercadoLivre" must return 0 after commit; acceptance criteria enforces this |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
After 2 commits:
- grep "content/mercadoLivre" src/server/index.ts returns 0 (dynamic import pitfall avoided)
- npx tsc --noEmit exits 0 (STRUCT-10)
- npm test exits 0
- src/adapters/scrapers/ now has PelandoScraper.ts, MercadoLivreScraper.ts, ProductScraper.ts (STRUCT-04 complete)
- src/content/ directory is gone
</verification>

<success_criteria>
- 2 atomic commits (D1, D2), each compilable before the next
- ZERO occurrences of 'content/mercadoLivre' remaining in index.ts
- npm test green
- src/adapters/scrapers/ complete (all 3 scrapers present)
</success_criteria>

<output>
Create .planning/phases/02-hexagonal-structure/02-04-SUMMARY.md when done
</output>
